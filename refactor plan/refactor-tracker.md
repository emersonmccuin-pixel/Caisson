# Refactor Plan Tracker

This tracker coordinates subsystem architecture handoff documents.

Subsystem documents belong in `refactor plan/refactor plan docs/`.

Use `refactor plan/subsystem-architecture-handoff-prompt.md` to create each subsystem document.

Use `refactor plan/target-architecture.md` as the north-star architecture for every subsystem document.

The target architecture is desired direction, not current implementation truth.

## Status Values

- `not started`: subsystem is identified but not documented.
- `in progress`: analysis is underway.
- `documented`: subsystem handoff doc exists.
- `needs synthesis`: doc has cross-subsystem conflicts or decisions to reconcile.
- `synthesized`: included in the holistic architecture synthesis.
- `planned`: implementation plan exists.
- `implemented`: implementation completed and verified.

## Current Refactor Priority

These are the subsystems of concern because they do not always work as expected.

Each priority subsystem document must include a `Target Architecture Alignment` section that compares the subsystem to `refactor plan/target-architecture.md`.

| Order | Focus | Tracker Subsystem | Target Doc | Why Priority | Notes |
|---|---|---|---|---|---|
| 1 | UI refresh / WebSocket system | WebSocket/event propagation | `refactor plan/refactor plan docs/ui-refresh-websocket-event-propagation.md` | Stale or missing UI updates make every other subsystem appear unreliable. | Establish event contracts, invalidation rules, frontend subscriptions, and ownership of projected state. |
| 2 | Chat | Chat runtime and transcript UI | `refactor plan/refactor plan docs/chat-runtime-and-transcript-ui.md` | Chat is the main runtime surface and currently mixes transcript, runtime state, tool events, pending prompts, and UI projection. | Decide whether chat is source of truth or a view over durable runtime/session events. |
| 3 | Agents | Agent runs | `refactor plan/refactor plan docs/agents-and-agent-runs.md` | Agent lifecycle, dispatch, recovery, transcript persistence, and control behavior are core app reliability concerns. | Include agent host, pods/orchestrator, verification, pause/resume, and active-run state as related areas. |
| 4 | Workflows | Workflows and workflow builder | `refactor plan/refactor plan docs/workflows-and-workflow-builder.md` | Workflows connect UI-authored plans, DAG execution, agents, work items, and durable run state. | Clarify whether workflows are orchestration records, templates, or both. |
| 5 | MCP and tooling | MCP bridge and MCP tools | `refactor plan/refactor plan docs/mcp-and-tooling.md` | Tool availability and MCP integration affect what agents can actually do. | Define canonical tool registry, tool availability, and app-owned vs external tools. |
| 6 | Channel server replacement | Channel server | `refactor plan/refactor plan docs/channel-server-agent-messaging-inbox.md` | Current channel behavior should be reconsidered as an app-owned agent/orchestrator messaging inbox. | Treat as replacement design: durable messages, delivery state, ack/read state, retries, ordering, and recovery. |
| 7 | Whole system at large | Holistic synthesis | `refactor plan/holistic-architecture-synthesis.md` | Reconcile the subsystem recommendations into one coherent architecture. | Run after the six priority subsystem docs exist. |

Lower priority for now:

- Desktop shell.
- Web app shell.
- Other supporting subsystems unless they become blockers during priority analysis.

## Planning-to-Build Workflow

The six priority subsystem handoffs and the holistic synthesis are complete. The next phase is to turn those findings into buildable, testable slices without losing cross-system coherence.

### Refactor Target Decision

The real implementation target is the current repo, not a blank rewrite in a new directory.

- Build new clean boundaries inside this repo.
- Use scratch directories only for disposable spikes or experiments.
- Preserve existing data/runtime/MCP/UI compatibility until replacement paths are tested.
- Do not start a parallel replacement app unless the user explicitly changes this decision.

### New Session Startup

Every new planning, implementation-planning, or build-slice session should start here:

1. Run `git status --short`.
2. If the worktree is dirty, stop and resolve the dirty state before starting new planning or implementation work unless the user explicitly asked to review, commit, stash, or clean it.
3. Read `AGENTS.md`.
4. Read `refactor plan/refactor-session-tracker.md` to identify the manual session row.
5. Read `refactor plan/definitive-session-pathway.md`.
6. Read `refactor plan/target-architecture.md`.
7. Read `refactor plan/holistic-architecture-synthesis.md`.
8. Read this tracker.
9. If a planning artifact is marked `in progress` or `not started`, read that artifact; otherwise read the artifact named by the next unchecked session row.
10. Read the build-slice plan named by the active session when one exists.
11. Inspect current code only for the scope being planned or implemented.
12. Update this tracker and `refactor plan/refactor-session-tracker.md` before ending the session.
13. Commit completed work or explicitly stash deferred work, then confirm `git status --short` is clean.

Current next action:

- Manual Session 24 is complete: slice 006 (conversation/send/replay seam) is built (contracts/db/app-services/server/web + tests; all gates green). Slice 006 build-slice plan is marked `building` below. **No DB migration / no schema change; the pending-interaction ask-shadow + `pending_interactions` table were DEFERRED to slice 007 per the human decision, and `/api/ask` is untouched.** The next unchecked manual row is Session 25 (human review slice 006) — human two-client/reconnect browser verification pending for the end-of-section pass; in-process equivalents cover the after-seq read, replay byte-identity, and send facade behaviors.
- Manual Session 21 is complete: slice 005 (agent-run command service) is built (contracts/db/app-services/server/web + tests; all gates green). Slice 005 build-slice plan is marked `building` below.
- The next unchecked manual row is Session 22 in `refactor plan/refactor-session-tracker.md` (human review slice 005). Human two-client browser verification is pending for the end-of-section pass; in-process equivalents are covered by server/web tests.
- NOTE: the slice-005 build also back-filled a slice-004 typecheck leak — `LiveEventEntity` (contracts), `LiveOutboxEntity` + schema `$type` (db) were never widened for `workflow-definition`/`workflow-run`/`workflow-review`, leaving `pnpm typecheck` RED and 2 app-services workflow tests failing on the slice-004 commit. The widening the slice-005 plan mandated (`agent-run`) was extended to those entities (additive, type-only), fixing the gate; the 2 workflow tests now pass.
- Manual Session 18 is complete: slice 004 (workflow definition/run/review service) is built (contracts/db/app-services/server/web + tests). Slice 004 build-slice plan is marked `building` below; human two-client browser verification still pending for the end-of-section pass.
- Slice 001 is fully verified and has no remaining slice-001 blockers (tag `slice-001-verified`).
- Slice 002 is implemented and verified/closed in Session 13 (tag `slice-002-verified`); browser test pending for the human end-of-section pass.
- Slice 003 is implemented and verified/closed in Session 16 (tag `slice-003-verified`); browser test pending for the human end-of-section pass.
- Session 17 done: slice 004 (workflow definition/run/review) build-slice plan created at `refactor plan/build-slices/004-workflow-definition-run-service.md` and marked `planned`; built in Session 18 (now `building`).
- The required session order is defined in `refactor plan/definitive-session-pathway.md`.
- Session 13 verified/closed slice 002 (tag `slice-002-verified`).
- Session 14 planned slice 003 at `refactor plan/build-slices/003-work-items-stages-fields-events.md`: work-item/stage/field/attachment contracts, compatibility adapters, a server-owned mutation gateway consolidating the bypassing verification/auto-advance/DAG/legacy paths, and durable `work-item.changed` / `stage.list.changed` events on the slice-002 `live_outbox`. The next unchecked session is Session 15: build slice 003 only.
- Slice 002 is fixed as durable `live_outbox` for `project.changed` only. Do not swap it for agents, workflows, mailbox, chat, or runtime work.
- Session 9 follow-up two-client verification found and fixed one slice bug: background project websocket sockets were being torn down on metadata-only project refetches, creating a short blind spot for back-to-back `project.changed` events. Reversible project rename/restore now propagates between two browser clients without refresh.
- Session 9 follow-up create verification found and fixed the attach-to-git scaffold bug: with no seed workflow YAMLs, create wrote only an empty `.project-companion/workflows/` directory, so `git commit -m Add Caisson scaffold` had nothing to commit. Scaffold creation now renders `setup-wizard-prompt.md` into `.project-companion/` and tolerates empty partial `.project-companion/` directories left by the failed attempt.
- Session 10 re-ran slice 001 verification. Automated tests/typechecks passed, and two isolated browser clients verified project create visibility, metadata rename/update, name restore, archive/delete visibility, and scaffold cleanup on the running dev stack without restarting dev processes.
- Session 11 created the slice 002 plan. The plan chooses wrapped canonical websocket frames `{ type: 'live-event', event }`, keeps the legacy top-level `project.changed` refetch hint during migration, uses global-only `project.changed` production events, and defers retention pruning.
- Session 12 built slice 002: canonical live-event contracts, additive `live_outbox` migration/repo, atomic project mutation plus outbox writes, replay route, dual canonical/legacy fanout, web cursor/replay/refetch handling, and focused tests. Automated gates passed. The already-running API returned 404 for `/api/live-events`, so browser verification against the dev stack remains for Session 13 after the stack has loaded the new server code; an in-process two-client fanout/replay test covers the missed-event recovery path without restarting processes.
- Do not treat manual Session 10 as roadmap Phase 10. Roadmap phases remain separate from manual workflow session numbers.

### Process Flow

| Step | Output | Purpose | Gate before moving on |
|---|---|---|---|
| 1 | `refactor plan/implementation-roadmap.md` | Convert synthesis into phases, PR boundaries, dependencies, rollback notes, and acceptance criteria. | Roadmap names build phases and first vertical slice without changing implementation code. |
| 2 | Foundation specs | Resolve the blocking architecture decisions that every build slice depends on. | Each spec defines schemas, ownership, migration phases, compatibility, and tests. |
| 3 | Work-item dependency handoff | Document work items/stages/fields/attachments because agents and workflows depend on them. | Agent/workflow plans can reference work-item contracts without guessing. |
| 4 | Phase 0 test characterization plan | Define the tests to restore or recreate before behavior refactors. | Test plan covers current WS, chat replay/send queue, agent asks, workflow review/cancel, MCP tools, and Channel delivery. |
| 5 | First build-slice plan | Pick a small vertical slice, likely shared contracts plus one low-risk feature. | Slice has scope, files, tests, rollback, compatibility, and stop conditions. |
| 6 | Build only when explicitly requested | Implementation follows the slice plan. | User explicitly asks to build; planning docs and tests/verification are sufficient for the slice. |

### Foundation Spec Queue

These specs should be concise and practical, not new long-form audits. Each should include decisions, proposed contract shapes, ownership, migration phases, acceptance criteria, tests, and open questions.

| Order | Artifact | Status | Output Path | Depends On | Exit Criteria |
|---|---|---|---|---|---|
| 1 | Implementation roadmap | planned | `refactor plan/implementation-roadmap.md` | Holistic synthesis | Ordered build phases, PR/checkpoint boundaries, first vertical slice, rollback strategy, and test gates are documented. |
| 2 | Shared contracts and app services spec | planned | `refactor plan/foundation specs/shared-contracts-and-app-services.md` | Implementation roadmap | Contract package rules, app-service boundaries, validation approach, MCP access rule, and first migrated feature are defined. |
| 3 | Live events and outbox spec | planned | `refactor plan/foundation specs/live-events-and-outbox.md` | Shared contracts direction | Canonical envelope, scope/global semantics, cursor/replay, outbox table, legacy adapter, and tests are defined. |
| 4 | Mailbox and pending interactions spec | planned | `refactor plan/foundation specs/mailbox-and-pending-interactions.md` | Shared contracts direction, Channel synthesis | Recipient identity, message/delivery/ack model, pending-interaction ownership, UI inbox, orchestrator-turn policy, and migration path are defined. |
| 5 | Runtime transcript and conversation store spec | planned | `refactor plan/foundation specs/runtime-transcript-and-conversation-store.md` | Shared contracts direction, chat handoff | Transcript repository strategy, file compatibility, SQLite mirror decision, replay cursor, send queue service boundary, and tests are defined. |
| 6 | Work items/stages/fields/attachments handoff | planned | `refactor plan/refactor plan docs/work-items-stages-fields-attachments.md` | Existing subsystem docs | Work-item contracts and stage/field/attachment dependencies for agents/workflows/MCP/live events are documented. |
| 7 | Phase 0 test characterization plan | planned | `refactor plan/phase-0-test-characterization-plan.md` | Foundation specs | Restore/recreate test inventory, current behavior characterization, known-bug tests, and CI/local commands are documented. |
| 8 | First build-slice plan | implemented | `refactor plan/build-slices/001-foundation-vertical-slice.md` | Roadmap and Phase 0 plan | Implemented in manual Session 9 and fully verified in Session 10. Scope remained limited to project list/project metadata shared contracts, a narrow project service seam, route parity, non-durable `project.changed` refetch, web contract/refetch integration, and focused tests. |

### Build-Readiness Gates

Do not start implementation refactors until the relevant slice has:

- an owning roadmap phase;
- explicit contracts or compatibility contract;
- current-state evidence from code;
- migration and rollback steps;
- test plan, including characterization tests where behavior is risky;
- a tracker update marking the artifact or subsystem `planned`;
- user confirmation that implementation should begin.

### Slice Shape

Every planned build slice should follow the same cartridge shape:

```text
contract
  -> app service / repo boundary
  -> route adapter
  -> live event or mailbox fact
  -> web client/hook
  -> MCP adapter when relevant
  -> tests
```

## Subsystem Inventory

| Subsystem | Status | Doc | Owner Area | Runtime Process | Baseline Branch | Baseline Commit | Migration Risk | Target Recommendation | Key Dependencies | Open Questions |
|---|---|---|---|---|---|---|---|---|---|---|
| Desktop shell | not started |  | `apps/desktop` | Electron main/preload |  |  | unknown | unknown | Web UI, server, dev controls |  |
| Web app shell | not started |  | `apps/web` | Renderer/Vite |  |  | unknown | unknown | API client, project state, chat surface |  |
| Shared contracts and app-service layer | planned | `refactor plan/foundation specs/shared-contracts-and-app-services.md` | future `packages/contracts`, future `packages/app-services`, `apps/server`, `apps/web`, `packages/mcp` | Server/renderer/MCP adapters | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | new foundational layer; zero-dependency browser-safe contracts first, server-side app services behind narrow ports, typed localhost MCP client first, project list/project metadata as first migrated feature | All priority subsystems, DB repos, domain, live events, MCP typed client | Canonical live-event/outbox semantics are deferred to the next foundation spec; project creation service ownership remains partly open because current create flow owns git/scaffold/runtime registration. |
| Chat runtime and transcript UI | synthesized | `refactor plan/refactor plan docs/chat-runtime-and-transcript-ui.md` | `apps/web`, `apps/server`, `packages/runtime`, `packages/db` | Renderer/server/runtime/Claude PTY | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | split/refactor; keep JSONL tailer, session rows, and send queue; add shared contracts, durable transcript/interactions, canonical live envelope | Runtime host, WebSocket/event propagation, agent runs/transcripts, workflows, MCP/channel, database | Should normalized transcript events move to SQLite directly or through a repository shim? Should pending asks belong to mailbox or pending-interactions? How should agent transcripts converge with chat replay? |
| Conversation/runtime transcript store | planned | `refactor plan/foundation specs/runtime-transcript-and-conversation-store.md` | future `packages/contracts`, future `packages/app-services`, `packages/runtime`, `packages/db`, `apps/server`, `apps/web` | Server/runtime/renderer/Claude PTY | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | new/refactor; keep `orchestrator_sessions` and `orchestrator_send_queue`, introduce file-backed `TranscriptRepository` first, treat SQLite transcript storage as mirror-only until parity tests pass, split transcript `seq` replay cursor from live outbox cursor, and expose a `ConversationSendService` facade for mailbox runtime turns | Chat runtime, agent transcripts, live events/outbox, mailbox pending interactions, runtime host, send queue, MCP typed contracts | Exact first after-seq route shape; whether SQLite mirror writes are synchronous or async; feature flag for mirror rollback; how `/api/ask` shadow pending interactions terminalize after server restart; first mailbox message kind to use `enqueueRuntimeTurn`. |
| Runtime host and PTY sessions | not started |  | `apps/server`, `packages/runtime` | Server/runtime process |  |  | unknown | unknown | Agent runs, terminal mode, project runtime |  |
| Agent runs | synthesized | `refactor plan/refactor plan docs/agents-and-agent-runs.md` | `apps/server`, `packages/runtime`, `packages/db`, `packages/domain`, `packages/mcp`, `apps/web` | Server/runtime/optional agent-host/Claude child/database | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | refactor behind shared contracts and an agent-run app-service boundary; keep DB-first run state; replace Channel delivery through mailbox later | Runtime host, agent host, WebSocket/event propagation, Channel/mailbox, work items, workflows, MCP, transcripts | Can paused runs survive server restart without host? Should `agent_runs` store resolved pod ID/scope? Should pending asks get a first-class UI surface? |
| Agent host process | not started |  | `packages/agent-host`, `apps/server` | External host process/server |  |  | unknown | unknown | Runtime host, agent runs, desktop shell |  |
| Pods and orchestrator | not started |  | `apps/server`, `packages/domain`, `packages/db` | Server/database |  |  | unknown | unknown | Agent runs, send queue, pod catalog, MCP tools |  |
| Workflows and workflow builder | synthesized | `refactor plan/refactor plan docs/workflows-and-workflow-builder.md` | `apps/server`, `apps/web`, `packages/workflows`, `packages/db` | Server/renderer/database | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | split/refactor; keep pure DAG package and current UI pieces; add durable definition/run/review services, shared contracts, active-run cancellation/recovery, and canonical live events | DAG executor, work items/stages, agents/agent host, WebSocket/event propagation, MCP, transient sessions, Channel/mailbox, database | Should runs reference workflow row/version or immutable definition revision? How should active workflow runs recover/cancel across restarts? Should `human-review` ship or be disabled until a durable inbox exists? Where should builder drafts live? |
| Work items, stages, and fields | planned | `refactor plan/refactor plan docs/work-items-stages-fields-attachments.md` | `apps/server`, `apps/web`, `packages/db`, `packages/domain`, `packages/mcp` | Server/renderer/database/MCP adapters | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | refactor around shared work-item, stage, field-schema, and attachment contracts plus a server-owned mutation gateway that emits canonical live events while preserving legacy HTTP/MCP/websocket compatibility | Projects, agents and agent runs, workflows, MCP, live events/outbox, mailbox/pending interactions, attachments/rich links | Stage-reference guarding, agent verification event emission, field-schema migration/revalidation, attachment size/provenance policy, and legacy route/tool compatibility semantics remain open. |
| Project lifecycle and registry | not started |  | `apps/server`, `apps/web`, `packages/db` | Server/renderer/database |  |  | unknown | unknown | Worktrees, settings, project context, runtime |  |
| Project worktrees | not started |  | `apps/server`, `packages/runtime`, `packages/db` | Server/filesystem/git |  |  | unknown | unknown | Projects, runtime sessions, agent runs |  |
| Files and project context | not started |  | `apps/server`, `apps/web` | Server/renderer/filesystem |  |  | unknown | unknown | Projects, chat, memory drawer, attachments |  |
| Settings and onboarding | not started |  | `apps/server`, `apps/web`, `packages/db` | Server/renderer/database |  |  | unknown | unknown | Project setup, auth/install checks, app settings |  |
| Dev controls and dogfood runtime | not started |  | `apps/server`, `apps/web`, `apps/desktop` | Server/renderer/Electron |  |  | unknown | unknown | Dev supervisor, ports, restart endpoints |  |
| MCP bridge and MCP tools | synthesized | `refactor plan/refactor plan docs/mcp-and-tooling.md` | `packages/mcp`, `apps/server`, `packages/runtime`, `packages/domain`, `packages/db`, `apps/web` | Server/MCP child/Claude runtime/renderer | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | refactor/split; keep pc-rig stdio adapter and session-local mcp config; add shared contracts, typed local client, canonical capability registry, and app-service command/query path | Agents/pods, agent runs, workflows, work items, runtime host, WebSocket/live events, Channel/mailbox, database | Where should the canonical capability registry live? Should MCP call app services directly or through a typed localhost client? Which subsystem owns `pc_ask_*` after mailbox/pending-interactions migration? How should external MCP server capabilities be discovered and validated? Does Channel replacement remove the orchestrator `webhook` MCP server? |
| MCP capability registry and external tool discovery | registry unified + live-verified (016 DONE); external discovery deferred | `refactor plan/build-slices/016-mcp-capability-registry-unification.md` | `packages/domain` (registry home), `packages/mcp` (handler map), `apps/web`, `packages/runtime` | MCP child/server/renderer | `dev` | `5bf64dff` | medium/high | ONE canonical `PC_RIG_TOOL_REGISTRY` in `@pc/domain` (name+inputSchema+label+description+family, canonical order) from which `TOOLS`/`PC_RIG_TOOL_NAMES`/`TOOL_CATALOG` pc-rig entries/`capabilities` all derive; `@pc/mcp` keeps only the name→handler map; parity test makes drift structurally impossible | MCP tools, pods, materializer, web agent settings, stock pod seed | RESOLVED by slice 016: registry lives in **`@pc/domain`** (catalog already there; contracts is wire-DTO only; both browser-safe → domain wins on cohesion + zero new dep). External MCP discovery / dynamic external-server tool listing is OUT of slice 016 (still open, deferred — capability discovery is a separable feature). |
| Statusline and usage telemetry | not started |  | `apps/server`, `apps/web`, `packages/db`, `packages/domain` | Server/renderer/database |  |  | unknown | unknown | Runtime sessions, usage caps, snapshots |  |
| Transient sessions | not started |  | `apps/server`, `apps/web`, `packages/db` | Server/renderer/database |  |  | unknown | unknown | Chat UI, runtime host, project state |  |
| WebSocket/event propagation | synthesized | `refactor plan/refactor plan docs/ui-refresh-websocket-event-propagation.md` | `apps/server`, `apps/web`, `packages/runtime` | Server/renderer/runtime | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | refactor/split | Runtime host, chat events, project state, work items, workflows, agents, channel/mailbox, live outbox | What is the canonical durable live-event envelope and cursor/replay contract? How should global events be scoped? Should runtime chat events stay file-backed or move behind SQLite live events? |
| Live event contracts and outbox | planned | `refactor plan/foundation specs/live-events-and-outbox.md` | `packages/contracts`, `packages/db`, `apps/server`, `apps/web`, future `packages/app-services` | Server/renderer/database | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | new foundational live-event layer; canonical `LiveEvent` envelope with `scope`, nullable `projectId`, global monotonic cursor, `live_outbox` table, replay API, legacy envelope adapters, and `project.changed` as first event family | WebSocket/event propagation, shared contracts/app services, projects, work items, workflows, agents, statusline, mailbox, runtime transcript store | First retention policy, whether first canonical WS frames are direct `LiveEvent` objects or `{ type: 'live-event', event }`, and whether `project.changed` emits global-only or both global and project-scoped events. Runtime transcript and mailbox event details are deferred to their specs. |
| Mailbox service and delivery workers | planned | `refactor plan/foundation specs/mailbox-and-pending-interactions.md` | future `packages/mailbox`, future `packages/contracts`, `packages/db`, future `packages/app-services`, `apps/server`, `apps/web`, `packages/mcp` | Server mailbox worker/renderer/runtime send service | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | new/replace; durable messages, typed recipients, delivery leases, retries, ack/read/action/dead-letter/audit, UI inbox, and orchestrator-turn delivery through the app send service facade | Channel server, agents, workflows, pending interactions, runtime send queue, live events, runtime transcript store | First mailbox route shape, first message kind to migrate, fallback flag shape, and whether agent `pending_asks` initially mirror into `pending_interactions` or expose through an adapter DTO. |
| Database and persistence layer | not started |  | `packages/db` | Database/repository layer |  |  | unknown | unknown | Domain models, server routes, migrations |  |
| Domain model package | not started |  | `packages/domain` | Shared TypeScript package |  |  | unknown | unknown | DB repos, server services, web types |  |
| Channel server | synthesized | `refactor plan/refactor plan docs/channel-server-agent-messaging-inbox.md` | `apps/server`, `channel-server`, `packages/db`, `packages/domain`, future `packages/contracts`/`packages/mailbox` | Server mailbox worker plus runtime send service; current per-Claude Channel bridge is legacy deletion target | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | replace with durable mailbox/message-inbox, UI inbox delivery, and app-injected orchestrator turns; delete Channel/dev-channel bridge | WebSocket/event propagation, chat pending interactions, agent-run pause/resume, workflow review delivery, MCP contracts, recipient identity model, runtime send queue/orchestrator turn injection, UI inbox | What is the canonical recipient identity? Should `pending_asks` be mailbox-owned or referenced by mailbox messages? What counts as acknowledgement for an injected orchestrator turn? Should external webhooks target active sessions, all sessions, user inbox, or project inbox? |
| Human review inbox and approval surfaces | not started |  | `apps/server`, `apps/web`, `packages/workflows`, future `packages/mailbox` | Server/renderer/database |  |  | high | unknown | Mailbox, workflows, work items, agents, chat UI | Code and stock prompts reference a Human Review inbox, but current guidance says `human-review` parks without an actionable surface; should this be merged with mailbox or built as a separate user inbox? |
| Pending interactions and approvals | planned | `refactor plan/foundation specs/mailbox-and-pending-interactions.md` | `apps/server`, `packages/db`, `packages/domain`, future `packages/contracts`, future `packages/app-services`, future `packages/mailbox`, `apps/web` | Server/renderer/runtime | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | high | new/refactor; pending-interaction state owns ask/review/approval lifecycle while mailbox delivers actionable references; keep agent `pending_asks` as compatibility until agent-run resume semantics move safely | Chat asks, agent `pending_asks`, workflow review, human review inbox, MCP `pc_answer_pending`, MCP `pc_complete_node`, mailbox, runtime transcript store | How blocking chat `/api/ask` moves to durable interactions, whether `pc_complete_node` eventually answers an interaction id or keeps `(runId,nodeId)`, and when `human-review` is rejected versus inbox-backed. |
| Attachments and rich links | planned | `refactor plan/refactor plan docs/work-items-stages-fields-attachments.md` | `apps/server`, `apps/web`, `packages/db`, `packages/domain` | Server/renderer/database | `dev` | `d114fc2535c1116f6eb2d883f9cac2a9193a8254` | medium/high | keep inline DB attachments initially, but define attachment DTO/provenance, canonical `attachment.changed` events, ownership checks, rich-link invalidation, and size/content/retention policy before heavier agent/workflow output use | Work items, chat transcript, agents/workflows, live events/outbox | Whether attachment mutations bump work-item aggregate versions or remain separate entity events; attachment size/content-type/retention limits. |

## Holistic Synthesis

| Document | Status | Inputs | Output | Notes |
|---|---|---|---|---|
| `refactor plan/holistic-architecture-synthesis.md` | synthesized | `refactor plan/target-architecture.md`, the six priority subsystem plans, `refactor plan/refactor-tracker.md`, targeted current-code checks | `refactor plan/holistic-architecture-synthesis.md` | Reconciles subsystem flow, conflicts, migration sequencing, and newly exposed supporting subsystems. |
| `refactor plan/refactor-session-tracker.md` | active | Manual prompt sequence and completion notes | `refactor plan/refactor-session-tracker.md` | Use this to track session-by-session progress through the planning-to-build workflow. |
| `refactor plan/definitive-session-pathway.md` | active | Roadmap phases, foundation specs, completed slice 001 evidence, required no-guessing session order | `refactor plan/definitive-session-pathway.md` | Controls required slice order and plan/build/verify session sequence from Session 10 through compatibility cleanup. |

## Planning Artifact Tracker

Use this table for the post-synthesis planning workflow.

| Artifact | Status | Output | Owner Area | Dependencies | Notes |
|---|---|---|---|---|---|
| Implementation roadmap | planned | `refactor plan/implementation-roadmap.md` | Whole system | Holistic synthesis | Created from tracker and holistic synthesis; next artifact is the shared contracts and app services foundation spec. |
| Shared contracts and app services spec | planned | `refactor plan/foundation specs/shared-contracts-and-app-services.md` | Contracts/app-services/server/web/MCP | Implementation roadmap | Created; chooses project list/project metadata as first migrated feature and typed localhost HTTP as the first MCP access rule. |
| Live events and outbox spec | planned | `refactor plan/foundation specs/live-events-and-outbox.md` | Live events/server/web/DB | Shared contracts direction | Created; settles canonical envelope, global/project scope, cursor/replay, `live_outbox`, legacy adapter migration, and `project.changed` as the first event family. |
| Mailbox and pending interactions spec | planned | `refactor plan/foundation specs/mailbox-and-pending-interactions.md` | Mailbox/agents/workflows/chat/MCP | Shared contracts direction, Channel synthesis | Created; defines mailbox as delivery state, pending interactions as action state, typed recipient addresses, UI inbox and orchestrator-turn policy, send-queue acceptance as first runtime-turn ack, and Channel compatibility rollback. |
| Runtime transcript and conversation store spec | planned | `refactor plan/foundation specs/runtime-transcript-and-conversation-store.md` | Chat/runtime/DB/web | Shared contracts direction, chat handoff | Created; chooses file-backed `TranscriptRepository` before storage migration, keeps `jsonl-events.jsonl` plus legacy `events.jsonl` compatibility, defines session-local transcript `seq` replay separately from live outbox cursor, keeps SQLite transcript storage mirror-only until parity tests pass, and defines `ConversationSendService`/`enqueueRuntimeTurn` as the mailbox-facing send boundary. |
| Work items/stages/fields/attachments handoff | planned | `refactor plan/refactor plan docs/work-items-stages-fields-attachments.md` | Work items/server/web/db/domain/MCP | Workflows, agents, MCP, live events | Created; captures current write paths, contract drift, stage/field risks, attachment provenance/lifecycle gaps, MCP compatibility issues, and live-event dependencies. |
| Phase 0 test characterization plan | planned | `refactor plan/phase-0-test-characterization-plan.md` | Test strategy/all priority subsystems | Foundation specs | Created; defines harness restoration, current coverage gap, P0/P1/P2 tests to restore or recreate, known-gap policy, local/CI commands, and slice-specific gates before behavior refactors. |
| First foundation build-slice plan | implemented | `refactor plan/build-slices/001-foundation-vertical-slice.md` | First implementation slice | Roadmap, Phase 0 plan | Implemented in manual Session 9 after explicit user confirmation and fully verified in Session 10. Added `@pc/contracts`, `@pc/app-services`, route/service parity, compatibility `project.changed` refetch, web client/hook typing, and minimal restored tests. No `live_outbox`, DB migrations, runtime/agents/workflows/MCP/mailbox/transcript changes, or dev-server restarts. Verification passed: `pnpm --filter @pc/contracts test`, `pnpm --filter @pc/contracts typecheck`, `pnpm --filter @pc/app-services typecheck`, `pnpm --filter @pc/server test`, `pnpm --filter @pc/server typecheck`, `pnpm --filter @pc/web test`, `pnpm --filter @pc/web typecheck`, and `pnpm typecheck`. Two-client browser verification on the running dev stack passed for project create visibility, metadata rename/update, name restore, archive/delete visibility, and scaffold cleanup. |
| Project live outbox build-slice plan | implemented | `refactor plan/build-slices/002-project-live-outbox.md` | Live events/server/web/DB/projects | Slice 001, live events and outbox spec, definitive session pathway | Created in manual Session 11 and built in manual Session 12. Scope stayed durable `live_outbox` for `project.changed` only: canonical live-event contracts, additive DB migration/repo, atomic project mutation/outbox publisher boundary, replay route, websocket dual fanout, web cursor/refetch behavior, tests, rollback, and stop conditions. Verified/closed in Session 13 (commit `5c2317b3`, tag `slice-002-verified`): all gates green; browser test pending for the human end-of-section pass. |
| Workflow definition/run/review build-slice plan | building | `refactor plan/build-slices/004-workflow-definition-run-service.md` | Workflow definitions/runs/review/server/web/live events | Slice 003, workflows handoff, live events and outbox spec, definitive session pathway | Built in manual Session 18 (commit `0eeef3a0`): contracts workflow-definitions.ts/workflow-runs.ts + canonical workflow.run/review/definition.changed; db additive listRunsByStatus + widened live_outbox entity union (no migration); app-services WorkflowRunMutationGateway (single durable write door) + adapters (snapshot-derived definitionHash) + fail-closed boot reconcile; server workflow-run-writer delegates to gateway, dag-run-service durable review facts, workflow-routes dual-emit workflow.definition.changed + legacy workflow-changed, index.ts workflow-run boot reconcile; web features/workflows/live-events.ts. Gates green (contracts 28/db 6/app-services 23/server 13/web 20, pnpm typecheck exit 0, git diff --check exit 0). Human two-client browser verification pending for the end-of-section pass. Next: Session 19 verify/close. Created in manual Session 17. Scope locked to workflow definition/run/review contracts, a single durable run-mutation write-door gateway over the `workflow-run-writer` announce path (mirrors the slice-003 `WorkItemMutationGateway`), formalizing the already-existing `workflowYamlSnapshot` as stable run identity (`definitionHash`) rather than reinventing it, node-boundary cooperative cancellation, fail-closed boot reconciliation for interrupted `running`/`pending` runs (additive `listRunsByStatus` read), and canonical `workflow.run.changed` / `workflow.review.changed` / `workflow.definition.changed` events on the slice-002 `live_outbox` with legacy `workflow-v2-run-changed` / `workflow-v2-review-pending` / `workflow-changed` projections. Compatibility routes preserved; no DB migration. Agent run (005), conversation/send (006), mailbox (007), and Channel cutover (008) are explicitly out of scope. Next session is Session 18: build slice 004. |
| Agent-run command service build-slice plan | building | `refactor plan/build-slices/005-agent-run-service.md` | Agent runs/pending asks/server/web/live events | Slice 004, agents-and-agent-runs handoff, live events and outbox spec, definitive session pathway | Built in manual Session 21: contracts `agent-runs.ts`+`pending-asks.ts` (AgentRunDto/PendingAskDto, invoke/continue/create/answer/cancel parsers, canonical `agent.run.changed` payload+guards+lossless legacy `agent-run-changed` adapter) and `live-events.ts` entity/type/accept-list widened with `agent-run`/`agent.run.changed` (and back-filled the slice-004 `workflow-*` entities/types that were referenced but never added to `LiveEventEntity`/`LiveOutboxEntity`/schema — see deviation); db `live_outbox.entity` `$type` (schema.ts) + `LiveOutboxEntity` widened (additive, NO migration) + `inArray` cast fix; app-services `agent-runs/` (`AgentRunMutationGateway` single durable write door: commitRunChange/announceRunChange/pauseRun/answerAndResume/cancelRun/commitTerminal — validate→persist→outbox insert in one tx→re-read post-write row→publication, rollback emits nothing; adapters toAgentRunDto/toPendingAskDto); server `agent-run-writer.ts` announcing seam (gateway + dual canonical+legacy fanout), routed factory state/host + terminal-effects + boot-reconcile + pause-resume pause/answer/cancel through it; web `features/agent-runs/live-events.ts` canonical acceptor + types alias. Three High bugs fixed (answer-ordering validate-before-flip + finalize-on-resume-failure; phantom-cancel durable finalize with no registry handle; host-mode stale-rev via post-write re-read). Canonical `agent.run.changed`, legacy `agent-run-changed`. No DB migration. Gates green: contracts 35/db 6/app-services 34/server 18/web 14, full `pnpm typecheck` exit 0 (incl. apps/web strict tsc -b), `git diff --check` exit 0. DEVIATION: the slice-004 commit left `pnpm typecheck` RED workspace-wide (the `LiveEventEntity`/`LiveOutboxEntity` unions were never widened for `workflow-*`, reducing the slice-004 intersection types to `never` and failing 2 app-services workflow tests; plus an `inArray` cast leak); the slice-005 plan mandates widening those exact unions, so I back-filled the workflow entities/types + fixed the cast to unblock the gate (additive/type-only; the 2 workflow tests now pass). Human two-client browser verification pending (Session 22) — dev stack must not be restarted; in-process server/web tests cover durable fanout, phantom-cancel, replay scope, web acceptor. Next: Session 22 human review. Created in manual Session 20. Scope locked to: an `AgentRunMutationGateway` single durable write door over the scattered `agent_runs` status/terminal broadcast sites (mirrors slice-003/004 gateways), pending-ask atomicity (answer validates resumability before the `open→answered` flip; pause writes ask+run+fact atomically; cancel finalizes a phantom paused run durably — closes the three handoff High issues + the host-mode stale-`rev` issue), canonical `agent.run.changed` (project-scoped, `version=agent_runs.rev`) on the slice-002 `live_outbox` with the legacy `agent-run-changed` record as a lossless compatibility projection, and pause/resume/cancel compatibility with both the in-process `AgentRun` and host-backed paths (host protocol/reattach/liveness sweep unchanged). Boot reconcile already exists — only its emission becomes a durable fact. No DB migration (additive `live_outbox.entity` `$type` widening only). `agent-jsonl-event`/transcript (006), mailbox/pending-interactions delivery (007), Channel cutover (008), runtime-host split (009), and MCP names (010) are explicitly out of scope. Docs-only; repo clean. Next unchecked row is Session 21: build slice 005. |
| Conversation send/replay build-slice plan | building | `refactor plan/build-slices/006-conversation-send-replay-service.md` | Conversation/session/send/replay/server/web/DB | Slice 005, runtime-transcript-and-conversation-store foundation spec, definitive session pathway | Built in manual Session 24 (built-but-not-yet-human-reviewed): READ + COMMAND-FACADE seam shipped (NOT a durable-write-door — chat is already a faithful JSONL render; transcript `seq`/`highWaterSeq` cursor already exists and is already separate from the slice-002 outbox cursor). **NO DB migration / no schema change. Pending-interaction ask-shadow + `pending_interactions` table DEFERRED to slice 007 per human decision** — `/api/ask` + its in-memory resolver + `chat-bridges/routes.ts` untouched. contracts: `conversations.ts`/`runtime-transcript.ts`/`runtime-send-queue.ts`/`runtime-hook-ask.ts` (all byte-identical wire mirrors; parse-only hook-ask still ships so 007 adopts the wire) + index. app-services `conversations/`: `FileTranscriptRepository` (read-only over the on-disk JSONL via an INJECTED `loadSessionReplayCheckpoint` reader + `listAfter` afterSeq trim), `ConversationReplayService`, `ConversationSendService` facade (send/enqueue/cancel/retry/observe via an injected `RuntimeTurnPort`; idempotent `enqueueRuntimeTurn` never raw-sends), adapters + index. db: additive `getOrchestratorSendByClientMessageId` over the existing unique index (no schema change). server: `services/conversation-replay.ts` (`loadRuntimeSessionReplay` delegates here → all `session-replay` surfaces byte-identical) + `services/conversation-send.ts`; `runtime-host/routes.ts` additive `?afterSeq=&limit=` read on `GET /sessions/:id/events` (per-session transcript seq cursor, NOT the global outbox cursor) + cancel/retry delegate to the facade (404/409 preserved); `websocket-message.ts` `handlePromptSend` delegates to `sendUserTurn` (send-ack statuses preserved). web: additive `runtimeApi.getSessionEventsAfter`. JSONL-canonical honored — `jsonl` stream stays transient, never on `live_outbox`; optional low-freq nudges left OUT. Gates GREEN: contracts 51 / db 6 / app-services 51 / server 23 (incl. new `runtime-host-routes.test.ts` after-seq + byte-identity parity) / web 14, full `pnpm typecheck` exit 0 (incl. apps/web strict tsc -b), `git diff --check` exit 0. DEVIATIONS (in-scope, minimal): (1) the planned web `chat-after-seq-replay.test.ts` was dropped — the chat reducer's value-imports use the `@/` alias the existing web tsx test harness can't resolve (no repo test imports `@/`); coalescing is covered by the server after-seq route test + the app-services repo test instead; harness widening is a slice-011 concern. (2) the live `maybeAdvanceSendQueueConfirmation` call site in `pty-handlers.ts` left as-is (facade `observeUserJsonl` tested for the future caller) to avoid touching the live PTY tailer. Human browser review (Session 25) pending — dev stack must not be restarted. Next: Session 25 human review. Created in manual Session 23. Scope is a READ + COMMAND-FACADE seam over the EXISTING orchestrator chat runtime (not a durable-write-door slice): `@pc/contracts` session/transcript-replay/send-queue/hook-ask DTOs mirroring the exact wire; a file-backed `TranscriptRepository` + `ConversationReplayService` wrapping `loadSessionReplayCheckpoint` over the on-disk `jsonl-events.jsonl` (legacy `events.jsonl` fallback) with byte-identical responses; an additive `?afterSeq=` read on the existing `/sessions/:id/events` route using the per-session transcript `seq` cursor (kept SEPARATE from the slice-002 global outbox cursor); a `ConversationSendService` facade over the existing `orchestrator_send_queue` repo + delivery helper exposing send/enqueue/cancel/retry/observe plus a mailbox-safe idempotent `enqueueRuntimeTurn` (no raw PTY send), injected via a `RuntimeTurnPort`; and a durable `pending_interactions` SHADOW row around the UNCHANGED in-memory `/api/ask` blocking resolver. Honors the JSONL-canonical constraint: the high-frequency `jsonl` transcript stream stays a transient projection and is NOT moved onto `live_outbox`; transcript history never enters `live_outbox.payload`. DB: at most ONE additive `pending_interactions` table for the ask shadow (or zero if the shadow is deferred to slice 007 — flagged STOP-and-confirm; this is the first slice that may add a migration, by the foundation spec's Phase 7 design). Optional low-frequency `conversation.session.changed`/`conversation.send-queue.changed` outbox refetch-nudges are left OUT by default (the pathway line for 006 names no live events). Mailbox (007), Channel cutover (008), runtime-host/transient-worktrees split incl. the deferred host-resume defect (009), MCP (010), and compatibility cleanup (011) are explicitly out of scope. Docs-only; repo clean. Next unchecked row is Session 24: build slice 006. |
| Mailbox platform build-slice plan | building | `refactor plan/build-slices/007-mailbox-platform.md` | Slice 006, mailbox-and-pending-interactions foundation spec, definitive session pathway | Built in manual Session 27 (built-but-not-yet-human-reviewed). The durable mailbox PLATFORM shipped additively ALONGSIDE Channel (no cutover). **FIRST real migration:** `packages/db/drizzle/0036_mailbox_platform.sql` — additive CREATE-only `pending_interactions` + `mailbox_messages`/`mailbox_recipients`/`mailbox_deliveries`/`mailbox_dead_letters`/`mailbox_audit` (no ALTER/backfill); journal idx 36; every `schema.ts` column verbatim in SQL; the fresh-DB migration test (`mailbox-migration.test.ts`) was written FIRST and is green (`assertSchemaIntact()` passes). contracts: `mailbox.ts`+`pending-interactions.ts` (DTOs, typed `MailboxAddress` parser, command parsers, canonical `mailbox.message.changed`/`mailbox.delivery.changed`/`pending-interaction.changed` payloads+guards) + `live-events.ts` widened (`mailbox-message`/`pending-interaction` entities + 3 new `LIVE_EVENT_TYPE_NAMES`). db: schema + repos `mailbox.ts` (atomic conditional-UPDATE lease + expiry reclaim, retry/accept/dead-letter, recipient read/action/dismiss) + `pending-interactions.ts` (version-bumping open→answered/cancelled/expired flips + boot-sweep). app-services `mailbox/`: `MailboxService` + `PendingInteractionService` single-transaction outbox-write-doors (scope-from-project: project-less → `scope:'global'`+null). server: `mailbox-orchestrator-turn-adapter.ts` WRAPS the sync `enqueueRuntimeTurn` facade (accepted⟺row, thrown⟺retryable, stable `mb:${deliveryId}` clientMessageId, never raw-send/Channel); `mailbox-worker.ts` lease→attempt→accept/retry/dead-letter; `features/mailbox/routes.ts` (`/api/projects/:projectId/mailbox/*` + app-level `/api/mailbox` + delivery inspector + answer); `ask-shadow.ts` durable `/api/ask` shadow via the injected `deps.askShadow` seam (in-memory resolver stays authoritative; boot-sweep orphans→expired). web `features/mailbox/`: client + `use-mailbox-inbox` hook + `MailboxInbox.tsx` + live helper (global inbox replays `includeGlobal`). NO `/api/live-events` route change (contract-driven). **Channel / `agent_inbox` / `enqueueAndPush` / `postChannel` / `/channel/*` / `pending_asks` UNTOUCHED; `compat-channel` ships unwired; cutover deferred to 008.** Gates GREEN 3× each: contracts 60, db 20, app-services 60, server 41, web 19 (deterministic); full `pnpm typecheck` exit 0; `git diff --check` clean; new test files type-clean. Human review (Session 28) pending. Next: Session 28. Created in manual Session 26. Scope is the durable mailbox PLATFORM, additive and pre-cutover: `@pc/contracts` mailbox message/recipient/delivery DTOs + typed `MailboxAddress` recipient union + pending-interaction DTOs + canonical `mailbox.message.changed`/`mailbox.delivery.changed`/`pending-interaction.changed` payloads on the slice-002 envelope; the FIRST real schema migration of the refactor — additive CREATE-only `0036_*` adding `pending_interactions` + `mailbox_messages`/`mailbox_recipients`/`mailbox_deliveries`/`mailbox_dead_letters`/`mailbox_audit` (additive + reversible, with explicit Drizzle ledger-lies / fresh-DB-boot safety mirroring `0035_live_outbox.sql` + `assertSchemaIntact` fresh-DB test); `MailboxService` + `PendingInteractionService` single-transaction outbox-write-doors (mirror the slice-005 `AgentRunMutationGateway`); a lease/ack/retry/dead-letter delivery worker whose `orchestrator-turn` channel rides the slice-006 `ConversationSendService.enqueueRuntimeTurn` facade (never raw-send, never Channel; `accepted` = send-service acceptance with a send-queue `target_ref`); the DEFERRED slice-006 `/api/ask` pending-interaction ask-shadow (durable shadow around the unchanged in-memory resolver, boot-swept to `expired`) as the first `pending_interactions` writer; the UI inbox surface; and the `/api/live-events` replay extension + web inbox live helper. Channel→mailbox CUTOVER of agent/workflow/webhook delivery is slice 008 (Channel stays fully in place; the `compat-channel` enum value ships unwired); agent `pending_asks` convergence, runtime-host split incl. the host-resume defect (009), MCP (010), and compat cleanup (011) are explicitly OUT. No implementation code changed. Next session is Session 27: build slice 007. |
| Channel cutover build-slice plan | building | `refactor plan/build-slices/008-channel-cutover.md` | Slice 007, mailbox-and-pending-interactions foundation spec, definitive session pathway | **Built in manual Session 30 (built-but-not-yet-human-reviewed).** The three delivery flows now route through the slice-007 mailbox behind three independent per-flow env gates (`PC_DELIVERY_AGENT`/`PC_DELIVERY_WORKFLOW_REVIEW`/`PC_DELIVERY_WEBHOOK`, `channel`\|`mailbox`), each DEFAULTING to `channel` (unknown/missing ⟹ `channel`) — with no env set, runtime delivery is byte-identical to today (verified by `delivery-routing.test.ts` + the gate=channel assertions in every flow test). The gate selects ONE path (no double delivery). NEW `apps/server/src/services/delivery-routing.ts` (per-flow resolver; injectable). Flow A: gated `deliverAgentEnvelope` helper in `agent-delivery.ts` (keeps `enqueueAndPush` intact) threaded via `features/agent-runs/routes.ts` → factory/terminal-effects/pause-resume; mailbox enqueues `orchestrator-session(projectId, dispatcherSessionId)` + `orchestrator-turn`, stable keys `agent:${runId}:${eventKind}` / `agent-ask:${pendingAskId}`. Flow B: a `WorkflowReviewDelivery` seam on `DagRunServiceOptions` + `orchestrator-review-step.ts`, threaded from `index.ts` → `ProjectRuntimeOptions.deliverWorkflowReview` → `ProjectRegistry` → `dagRunOptions()` (ONE injected fn; no runtime-host refactor — friction #1); mailbox enqueues `active-orchestrator` + `orchestrator-turn`, key `workflow-review:${runId}:${nodeId}`; slice-004 `workflow.review.changed` fact + legacy `workflow-v2-review-pending` broadcast fire in BOTH positions. Flow C: a `webhookSink` seam on `ChannelServerDeps`; mailbox enqueues durable `external-webhook` (`project-inbox` + `ui-inbox`, no silent drop on missing registrant), key `webhook:${slug}:${source}:${hash}:${at}`; the `channel-event` UI broadcast (`onEvent`) fires in BOTH positions; bridge fan-out skipped only on the mailbox path. Friction #3 (boot-order): the boot-reconcile/liveness/host-reconcile terminal emitters predate `mailboxService` and pass `channelServer` with NO mailbox port → `deliverAgentEnvelope`'s `&& deps.mailboxEnqueue` guard keeps those recovery-path notices on Channel even when gated mailbox (documented; low-volume recovery-only). Per-CC bridge: a mailbox orchestrator-turn delivers a runtime turn via the send facade, not a `channel-event`; the bridge stays for any `channel`-gated flow. NO DB migration (slice 007 added every table). Channel / `agent_inbox` / `/channel/*` / `enqueueAndPush` / `postChannel` / `forwardToProjectChildren` / per-CC bridge / `compat-channel` / `pending_asks` NOT deleted — Channel is the default + fallback (deletion is slice 011). Reuses slice-007 `MailboxService`/`MailboxWorker`/`MailboxOrchestratorTurnAdapter` — no new primitive. Tests (all NEW, type-clean via temp tsconfig): `delivery-routing.test.ts`, `agent-delivery-gate.test.ts`, `workflow-review-gate.test.ts`, `webhook-gate.test.ts`. Gates GREEN: `@pc/contracts` 60, `@pc/db` 20 (3×), `@pc/app-services` 60 (3×), `@pc/server` 59 (3×, deterministic), `@pc/web` 19; all typechecks + full `pnpm typecheck` exit 0; `git diff --check` clean. DEVIATIONS (in-scope, minimal): (1) Flow B does NOT create a `pending_interactions` `workflow-orchestrator-review` row — plan §16 flagged it recommended-but-confirm/OPTIONAL; deferred to keep the cutover a pure sender re-point (the slice-004 fact already provides the inspectable projection). (2) `orchestrator-review-step.ts` has no live caller (the live path is `dag-run-service.ts requestReview`); its `deliverReview` seam is added/tested for parity, not exercised in production. Human review (Session 31) pending — dev stack not restarted. — Created in manual Session 29. Scope is the gated, fallback-preserving cutover of the THREE delivery flows from Channel onto the slice-007 mailbox (foundation spec §10 Phases 6/7/8): (A) agent delivery — the 4 `enqueueAndPush` sites (`agent-run-factory.ts:1016` queued-started, `agent-run-terminal-effects.ts:274` completed/failed, `pause-resume.ts:181` asks) → `MailboxService.enqueue` (`orchestrator-session` + `orchestrator-turn`); (B) workflow review — `dag-run-service.ts:666` `requestReview` + `orchestrator-review-step.ts:53` `postChannel` → mailbox `workflow-review` (`active-orchestrator` + `orchestrator-turn`), slice-004 `workflow.review.changed` fact unchanged; (C) external webhook — the `/channel/:slug/:source` listener (`channel-server.ts:73`) → durable mailbox `external-webhook` (`project-inbox` + `ui-inbox`, no silent drop), `channel-event` UI broadcast kept. GATING SWITCH: three independent per-flow config keys (`PC_DELIVERY_AGENT`/`PC_DELIVERY_WORKFLOW_REVIEW`/`PC_DELIVERY_WEBHOOK`, `channel`|`mailbox`), each DEFAULTING to `channel` (unknown/missing ⟹ `channel`), so a fresh checkout is byte-identical to today; the gate selects ONE path (no double delivery; optional `shadow` mode opt-in only). Stable idempotency keys per flow make every flip safe (at-most-one delivery). Channel / `agent_inbox` / `/channel/*` / `enqueueAndPush` / `postChannel` / per-CC bridge / `compat-channel` are KEPT as gated fallback — NOT deleted (deletion is slice 011). No DB migration (slice 007 added every table). Reuses the slice-007 `MailboxService`/`MailboxWorker`/`MailboxOrchestratorTurnAdapter` — no new delivery primitive. Verified-from-code contradiction flagged: the Flow-B `postChannel` is composed deep in `ProjectRuntime.dagRunOptions()` (`project-runtime.ts:265`, no `mailboxService` ref) — the cutover adds ONE injected review-delivery seam without a runtime-host refactor (reserved for 009); the per-CC bridge consumer (`channel-server/server.js`) receives a runtime turn (not a `channel-event`) under the mailbox path; and the agent boot-reconcile/liveness/host-reconcile terminal emitters run before `mailboxService` is constructed (boot-order risk). Runtime-host split incl. host-resume defect (009), MCP (010), slice-011 Channel deletion, agent `pending_asks` convergence, and the slice-004 boot-reconcile gap are explicitly OUT. Docs-only; repo clean. Next session is Session 30: build slice 008. |
| Runtime-host boot-gate / host-resume / echo-recovery build-slice plan | implemented | `refactor plan/build-slices/009-runtime-host-transient-worktrees.md` | Slice 008, agents-and-agent-runs handoff, runtime-transcript foundation spec, definitive session pathway | Created in manual Session 32 (slice 009 planning), grounded in current code. REPRIORITIZED around three confirmed runtime-host defects (the pathway's nominal runtime-host/transient-worktree split is scoped DOWN to what these require; the rest is DEFERRED to slice 011). OBJ-1 (LEAD): the agent→mailbox boot-order gate-wiring — the boot-reattach (`index.ts:371`), reconcile sweep (`:413`), and liveness sweep (`:442`) handlers omit `deliveryRouter`+`mailboxEnqueue` (constructed at `:477`/`:538`/`:540`), so they apply LIVE host terminals via Channel and win the idempotency race vs the factory's gated handler (hotfix `e22456d2` necessary but insufficient). Chosen MECHANISM = (a) construct the mailbox bindings BEFORE the boot handlers and pass the port directly (TDZ-safe; the inline `await reattachAgentRunsDuringServerBoot` at `:371` can apply a terminal synchronously, so a lazy getter (b) would risk a boot-time TDZ throw). Thread the two deps through `agent-run-liveness-sweep.ts finalize`. The boot deps (`AgentHostReattachDeps`/`AgentRunServerBootDeps`) ALREADY accept the params — only the call sites omitted them. OBJ-2: host-backed resume drops the answer — `answerPendingAsk` → `HostBackedActiveRunHandle.resumeWithAnswer` → host `answer-pending` → `AgentRun._resumeWithAnswer` → `runSpawnPhase('resume', answer)` → `spawn.send`; fix = host returns typed `not-resumable` (instead of stale-snapshot `ok`) + server maps to `resume-failed` so the run never strands `running`; exact branch (paused-state loss vs send-never-submits) needs a live host `getState()` trace to disambiguate (flagged). OBJ-3: `send-protocol.ts sendBracketedPaste` returns `echo-timeout` at `:201` WITHOUT `\r`, leaving the body in the composer (the gluing root); fix = CLEAR the composer (Ctrl-U `\x15` or Escape `\x1b`) before returning, never blind-submit. No DB migration; no contract change expected (possible additive agent-host `not-resumable` error code). Tests extend `agent-host-terminal-gate.test.ts` to the boot-handler paths + restore `send-protocol.test.ts` (note: `test/**` excluded from `pnpm typecheck` — type-check via temp tsconfig). OUT/DEFERRED: broader runtime-host interface/transient-session/worktree-path-guard facade (011), `/agent-runs/{id}/cancel` host-not-aware 404 (separate), 004 cancel surface + unwired workflow boot-reconcile, Channel deletion (011), Q2 mailbox-as-pending-interaction-authority (own slice). **BUILT Session 33 (OBJ-1/2/3) + reviewed Session 34 (2026-05-31): IMPLEMENTED.** Session-34 review live-traced OBJ-2 and found it UNREACHABLE — a fresh host-backed `pc_ask_user` was rejected `409 wrong-state` forever because the pause gate read the in-memory `HostBackedActiveRunHandle.snapshot` (never advanced to `running`; only the reconcile sweep updated the DB row). **Added + built OBJ-2A** (plan §"OBJECTIVE 2A"): pause/resume gates now decide on the RECONCILED DB row (`getAgentRunRow`) + on-demand host `list-runs` level-read; sweep + `applyAgentHostEvent` re-seed a registered host handle; factory init-order race closed (handle demoted from state authority per ADR "single owner / reconcile-is-correctness"). No DB migration, no contract change. Gates (self-run): full `pnpm typecheck` exit 0; `@pc/server` 81/81; `git diff --check` clean; new tests `agent-pause-resume.test.ts`+`agent-host-reattach.test.ts`. **Live-verified on the UI** (gated stack reloaded): dispatch `researcher` → `pc_ask_user` → `paused` → answer "marmalade" → resume → `completed` (run `01KT05CWFVB684ME6WM1MHF9KY`). Next session is Session 35: plan slice 010 (Areas). |
| Work items stages fields events build-slice plan | implemented | `refactor plan/build-slices/003-work-items-stages-fields-events.md` | Work items/stages/fields/attachments/server/web/MCP/live events | Slice 002, work-items handoff, definitive session pathway | Created in manual Session 14. Built in manual Session 15: `@pc/contracts` work-item/stage/field-schema/attachment DTOs + request parsers + guards; `live-events.ts` entity union + canonical type names widened (`work-item.changed`/`stage.list.changed`/`field-schema.list.changed`/`attachment.changed`) and `parseListLiveEventsQuery` updated; `@pc/db` `live_outbox.entity` TS type widened (additive, no migration); `@pc/app-services` `work-items/` adapters (bidirectional row<->DTO + `{workItems}`/`{items,nextCursor}` normalizer, fail-loud) and `WorkItemMutationGateway` (validate -> persist -> outbox insert in one txn -> post-commit canonical+legacy publication; rollback emits nothing) covering create/patch/move/delete/restore + verify/approve/reject/auto-advanced + stage replace + field-schema replace + attachment create/delete; `/api/live-events` replay extended for project-scoped work-item/stage events; web canonical-frame acceptors added alongside legacy envelope guard. All focused tests (contracts 18, db 5, app-services 12, server 13, web 5) + package typechecks + full `pnpm typecheck` + `git diff --check` GREEN; in-process gateway-outbox + replay-route tests stand in for the two-client browser pass. DEFERRED to Session 16 (verify/fix): rewiring the bypassing verification/auto-advance/DAG/legacy fields-only call sites to announce through the gateway at the server composition layer (gateway + contract in place + tested; call-site rewires touch the live runtime fanout layer and need browser verification). Next session is Session 16: verify/close slice 003. |
| State-propagation spine build-slice plan | planned | `refactor plan/build-slices/015-state-propagation-spine.md` | WebSocket relay / outbox door / per-subsystem migration / no-bypass gate | Slice 009 (state-propagation principle applied locally), state-propagation ADR (`archive/docs/state-propagation-decision.md`), slices 002–008 outbox gateways | Created 2026-05-31 (opus planner, code-grounded). Makes UI staleness structurally impossible + moves EVERY subsystem onto one door. **Key finding:** the outbox (`live_outbox`) already exists with the two counters (PK `seq` = global cursor, `version` col = per-entity rev) + replay route + contracts; the REAL gap is there is **no relay** — delivery today is hand-written dual-emit (gateway inserts the outbox row in-txn, then the call site SEPARATELY calls `broadcastTo`/`broadcastAll`), the WS hub (`websocket-hub.ts`) is pure fanout with NO cursor/replay/buffer, and only `project.changed` replays on reconnect (`App.tsx`), so a blipped tab silently misses every other domain's updates — THE staleness bug. **Decision: PROMOTE `live_outbox` to the canonical door (do NOT add a `changes` table — it would duplicate seq/version/replay/contracts).** Sub-slices: **015a** = the relay (drain committed outbox → sockets) + the exact ADR WS `lastVersion` subscribe handshake (snapshot max seq → replay `(lastVersion, snapshot]` → attach listener → dedupe by version+id → `resetRequired` full-reload fallback) + size/age prune; **015b** = migrate the ~24 bypassers (mailbox, pending-interactions/"Waiting on you", orchestrator runtime + send-queue, field-schema, project-claude-md, transient-session, etc.) onto the relay, delete each ad-hoc broadcast, one subsystem per commit; **015c** = static-search no-bypass gate (no `broadcast*`/`fanoutMessage` outside the relay) + pass-through allowlist — **subsumes slice 012 (compatibility cleanup); do NOT also write a separate 012**. Pass-through tier explicitly EXEMPT (the "better way" cases): live PTY output, `run-chunk`/`run-jsonl` byte-spam, the file-tailed chat transcript. **Recommended execution order: 010 → 015a → 015b → 015c → 011** (foundational; ADR steps 1–5 already done — agent-run `rev` + agent-host reconcile landed in slices 005/009, so the ADR's "agent-runs missing rev" anchor is stale). Reconcile/relay-first: ship catch-up BESIDE old paths, verify, then delete per subsystem. Docs-only; repo clean. |
| MCP typed client / capability registry build-slice plan | implemented | `refactor plan/build-slices/011-mcp-typed-client-capabilities.md` | MCP adapter / capability registry / typed localhost client / external MCP config | Slices 003/004/005/010 contracts (consumed, not added), slice 015 relay (no-bypass gate) | **IMPLEMENTED + LIVE-VERIFIED 2026-06-01 (Session 40, Claude drove the review via Playwright + API on the running gated stack — NO restart needed: wire frozen so the server's `PC_RIG_TOOL_NAMES` is byte-identical, and the slice-011 tool code lives in the per-agent `dist/server.mjs` bundle rebuilt during gates).** Live checklist ALL PASS: (1) Start Chat → orchestrator's `pc-rig` MCP came up **"52 tools"** = the rebuilt bundle (with the new `@pc/contracts` dep) boots + completes the JSON-RPC handshake; (2) full roster intact — 52 == frozen TOOLS count, `mcp-status.json` confirms `pc_list_areas` present + order preserved; (3) tool OUTPUT byte-identical: `pc_list_work_items`→raw `{"workItems":[…]}` passthrough == API body, `pc_list_my_runs`→`{"ok":true,"runs":[]}`, `pc_list_workflows`→`ahead-pipeline` def clean; (4) `pc_create_work_item` persisted server-side (API-confirmed, callsign ahead-6) via the typed client's POST + the second tab (never reloaded) rendered it via the relay. Test data cleaned up (probe soft-deleted). **Incidental finding (NOT a slice-011 regression, NOT in scope): the orchestrator's allowed-tools KIT omits `pc_list_areas`** — the MCP server exposes it (52 tools incl. it) but the orchestrator pod's kit was never updated when slice 010 added the tool; pre-existing slice-010 pod-kit gap, flagged for separate follow-up. **BUILT in manual Session 39, commits `3ae102cc`..`8c90ac6a` on `refactor/auto-pathway`.** WIRE FROZEN held: tool names / ListTools order / input schemas / HTTP payloads+paths / agent-visible result strings are all byte-identical (proven by golden text-compat tests written FIRST per family + the 11G spawned-bundle ListTools-order smoke). **Foundation (`3ae102cc`):** added `@pc/mcp → @pc/contracts` workspace dep + a `test` script (`tsx --test "test/*.test.ts"`); 11A derived parity-guarded `CAPABILITIES` registry (`src/capabilities.ts`, name→{family}) with a full TOOLS↔registry bijection test + `PC_RIG_TOOL_NAMES` derivation assertion (TOOLS stays SOLE source; registry is lookup-only); 11B `TypedLocalhostClient` (`src/client/typed-client.ts`) wrapping the INJECTED raw `ToolContext` helpers (no `node:http` re-impl), returning the parsed contract DTO + a typed error AND always the raw `{status,body}` compat fallback, exposed off `ToolContext.client`. **11C (`ec0ad5bc`):** work-items `pc_create/get/list/move/update_work_item` + `pc_list_areas` + `pc_attach_to_work_item` and project-config `pc_get_stages`/`pc_list_stages` (parses `StageDto[]` off the project doc; custom MCP stage projection output unchanged) + `pc_list_field_schemas` routed through `ctx.client`; emit raw `res.body` verbatim. **11D (`7daf7590`):** `pc_fire_workflow`→`client.fireWorkflow` (WorkflowRunDto), `pc_get_workflow`→`client.getWorkflow` (WorkflowDefinitionDto). **11E (`bf6304b8`):** `pc_invoke_agent`/`pc_continue_agent`→AgentRunDto, the pause/ask trio (`pc_ask_orchestrator`/`pc_ask_user`/`pc_request_approval`)→`client.createPendingAsk` (PendingAskDto), `pc_answer_pending`→`client.answerPendingAsk`. **LEFT RAW + noted (no covering DTO; STOP-and-confirm not triggered):** all of `agents.ts` pod-CRUD (`pods.ts` models only live-events, not CRUD response bodies — documenting note added); `pc_list_my_runs`/`pc_inspect_agent_run`/`pc_kill_agent_run`; workflow publish/create/update/delete/save-draft/read-draft/list/complete-node/node-failed; project-config claude-md/replace-stages/replace-field-schemas. **11F (`9669507d`):** hardened `parsePodMcpServerConfig` SHAPE validation (reject non-object incl. arrays, empty command/url, neither-command-nor-url, both, args/env-with-url) — no valid stdio/http config's acceptance or output changed; capability discovery stays OUT; rewriter suffix-match untouched. **11G (`8c90ac6a`):** `test/bundle-smoke.test.ts` spawns `dist/server.mjs` with fake `PC_*` env + drives initialize→tools/list, asserting ListTools == TOOLS order (proves the contracts dep doesn't break the bundle). NO new emit path (no `broadcast`/`live_outbox`/`@pc/db` in `packages/mcp`); NO DB migration; NO contract/route/app-service behavior change; NO new `@pc/contracts` parser. Gates GREEN (self-run, repo root): `@pc/contracts` test 68; `@pc/mcp` typecheck exit 0 / build → dist/server.mjs 690.3kb / test 46; `@pc/server` test 121 / typecheck exit 0; `@pc/web` typecheck exit 0; full `pnpm typecheck` exit 0; `git diff --check` exit 0. New test files run via `tsx --test` (the `pnpm typecheck` `src/**`-only gap) and are type-clean. Human review (Session 40) pending — dev stack not restarted; repo clean. — Created 2026-06-01 (opus planner, code-grounded against `5bf64dff`). Routes `@pc/mcp` tool internals through existing `@pc/contracts` DTOs/parsers + a typed localhost client behind a derived capability registry — **WIRE FROZEN**: no tool-name / ListTools-order / input-schema / HTTP-payload / agent-visible-result-string change. **Two locked decisions:** (1) **`@pc/mcp` DOES take a `@pc/contracts` dep** — contracts are isomorphic/zero-runtime-dep (no `node:` imports; verified) and already imported as `workspace:*` source by `@pc/app-services` under the same Bundler resolution `@pc/mcp` uses, esbuild `bundle:true` inlines them with zero footprint (build.mjs → dist/server.mjs unaffected); cross-check = `apps/server` typechecks against `@pc/mcp` src (imports `PC_RIG_TOOL_NAMES`), covered by `pnpm typecheck`. (2) **`TOOLS` (server.ts:84-126) stays the SOLE source of truth**; the registry (11A) is a derived, parity-guarded `name→{family,parser,buildText}` lookup — inverting it would risk reordering TOOLS + drifting `PC_RIG_TOOL_NAMES`/`pod-tool-catalog` (Section-36 drift trap). 11B typed client lives in `packages/mcp` (MCP-process-specific localhost HTTP), wraps the raw `ToolContext` helpers, returns parsed DTO + typed error + **keeps the raw `{status,body}` fallback per family** (compat column). Migration one-commit-per-family: 11C work-item/project → 11D workflow → 11E agent/agent-run/pending; "migrated" = byte-identical result strings (golden tests written first). 11F = harden `pod-mcp-config.ts`/`mcp-config-rewrite.ts` shape validation (capability *discovery* OUT). 11G = `pnpm --filter @pc/mcp build` bundle smoke (hard gate). **No new emit path** — MCP mutates only via apps/server routes (the slice-002/003/004/005/010 gateways own the `live_outbox` door + the 015 relay), verified no `broadcast`/`live_outbox`/`@pc/db` in `packages/mcp` → 015c no-bypass gate stays green. No DB migration; no contract/route/app-service behavior change. STOP-and-confirm flags: adding any new `@pc/contracts` parser (e.g. agent-pod CRUD response — default = leave raw + note); generating JSON Schema from contracts (deferred); external-server capability discovery (deferred). Docs-only; repo clean. |
| MCP capability registry unification build-slice plan | implemented (live-verified) | `refactor plan/build-slices/016-mcp-capability-registry-unification.md` | MCP capability registry / pc-rig tool metadata / `@pc/domain` registry / `@pc/mcp` handler map | Slice 011 (started the consolidation — added derived `capabilities.ts` but kept three sources), `@pc/domain` `tool-catalog.ts`, `@pc/mcp` `server.ts` `TOOLS` + `tools/*.ts` | **IMPLEMENTED + LIVE-VERIFIED 2026-06-01 (Session 58, Claude drove the review: fresh `pc-rig` spawn pid 33092 reports 52 tools incl. `pc_list_areas` in frozen `TOOLS` order — registry→TOOLS derivation byte-order-identical in a real spawn; output byte-identity carried from the 011 review since handlers are untouched). BUILT 2026-06-01 on `refactor/auto-pathway`, commits `42239ee8`/`c35ffb22`/`74c81cd1`/`f90a0650` (4 logical moves per §12).** ONE canonical `PC_RIG_TOOL_REGISTRY` now lives in `@pc/domain/tool-registry.ts` (52 records, exact ListTools order): `{ name, family, label, description (agent-facing), catalogDescription (UI), inputSchema }`. **Two description fields were REQUIRED — verified 0/52 byte-identical** between the agent-facing `*_TOOL.description` and the UI `TOOL_CATALOG.description`, so every record carries both; none collapse to one (the §6/§16 STOP-and-confirm case was checked and the field model holds losslessly). `TOOLS` + `PC_RIG_TOOL_NAMES` (server.ts) now zip from the registry in registry order; `@pc/mcp` keeps ONLY `tools/handlers.ts::PC_RIG_HANDLERS` (name→fn) which WRAPS the existing `handleXTool` chain (dispatch byte-identical, chain-collapse deferred per §8/§16); the 52 inline `*_TOOL` consts + per-family arrays were removed from `tools/*.ts` (handlers/helpers kept). `TOOL_CATALOG` pc-rig partition + `capabilities.ts` family map both DERIVE from the registry; cc-builtin/mcp-server catalog entries + `REQUIRED_AGENT_TOOLS`/`mergeRequiredAgentTools` stay hand-authored. **Parity test** `packages/mcp/test/registry-parity.test.ts` (8 cases) makes handler keys === registry names === capabilities keys === PC_RIG_TOOL_NAMES (stripped) === TOOL_CATALOG pc-rig slugs (stripped) a bijection in registry order — half-adding a tool fails the build. **Golden byte-identity** `registry-golden.test.ts` (7 cases, written FIRST, snapshot `test/__golden__.json`) freezes ListTools `{name,description,inputSchema}`+order, PC_RIG_TOOL_NAMES, the full TOOL_CATALOG, and descriptionOf/friendlyName/lookupTool outputs. **Add-a-tool touch points: 2** (one registry record + one handler), down from 4. Registry tests COLOCATED in `@pc/mcp` (imports both packages; its `tsx --test` runner exists) — no `@pc/domain` test script added. **Deviations (in-scope, behavior-neutral):** (a) mcp imports the registry via a NEW barrel-free `@pc/domain/tool-registry` subpath export — a `@pc/domain` barrel import bundles `yaml` (dynamic CJS require) and breaks the esbuild `dist/server.mjs` boot; `tool-registry.ts` is import-free pure data so the dist stays clean. (b) the TOOL_CATALOG array now lists the pc-rig partition in registry order (was a separate hand order) and groups all cc-builtins together (AskUserQuestion moved adjacent to the other 9) — behavior-neutral: every consumer is slug-keyed (`descriptionOf` etc.), web doesn't import the catalog array order, and the golden test proves all per-slug values + lookup outputs are byte-identical. NO new emit path (015c gate untouched), NO DB migration, NO `@pc/contracts` change, NO node:/SDK/broadcast import into `@pc/domain`. **Gates GREEN (self-run, repo root):** `@pc/contracts` test 68; `@pc/domain` typecheck 0; `@pc/mcp` typecheck 0 / build → dist/server.mjs 702.3kb (boots, ListTools=52 in order) / test 61; `@pc/server` test 121 / typecheck 0; `@pc/web` typecheck 0; full `pnpm typecheck` 0; `git diff --check` 0. Human review pending — dev stack NOT restarted; repo clean. — Created 2026-06-01 (opus planner, code-grounded against `5bf64dff`). **Finishes what 011 started:** collapse the THREE hand-maintained pc-rig tool sources + the derived names array into ONE canonical parity-guarded registry so adding a tool is ONE edit (+ one handler) instead of FOUR, and the "added to one list, missing from the others" bug class becomes structurally impossible. **Motivating drift (verified):** `TOOL_CATALOG` (`tool-catalog.ts`) is hand-maintained and HAS drifted — `pc_node_failed` + 8 tools "never added" (comment `:150-153`), `pc_list_areas` missing until slice 010's fix. All three sources sit at 52 today, kept in sync by hand: `TOOLS` (`server.ts:84-126`, 52 `*_TOOL` consts), `TOOL_CATALOG` (52 `source:'pc-rig'`), `capabilities.ts` (52 keys), + `PC_RIG_TOOL_NAMES = TOOLS.map(...)`. **Locked decisions:** (1) **Registry home = `@pc/domain`** (NOT contracts) — the catalog already lives there; contracts is wire-DTO/parser only; both browser-safe so domain wins on cohesion + zero new dep (`@pc/mcp` already deps domain). (2) **inputSchema moves IN** — registry holds name+inputSchema+label+description+family in canonical order, becoming the whole agent wire; `packages/mcp` keeps ONLY a handler map (name→fn) and zips registry+handlers into `TOOLS` IN REGISTRY ORDER (so ListTools order is guaranteed by the single ordered source, not a hand array). Bigger one-time move (schemas are inline `as const` in `tools/*.ts`) but it's the only way to hit "one edit". (3) `TOOL_CATALOG` pc-rig partition / `PC_RIG_TOOL_NAMES` / `capabilities` all DERIVE; cc-builtin + mcp-server catalog entries + `REQUIRED_AGENT_TOOLS`/`mergeRequiredAgentTools` stay hand-authored. **Parity guard** = handlerMap keys === registry names === catalog pc-rig slugs === capabilities keys === names, in registry order — half-adding a tool FAILS the build. **The boundary fact:** the handler is executable HTTP-issuing code (can't go browser-safe); metadata is plain data (can) → split by purity. **WIRE FROZEN** (inputSchema relocation byte-identical; names/order/payloads/result-strings unchanged) + **No new emit path** (015c gate stays green) + **No DB migration** (code-not-DB locked; DB keeps owning grants via `pods.tools_json` + external mcp-server configs only) + no contract/route change. **Execution order: BEFORE 013/014** (user in-session direction). **STOP-and-confirm:** if the agent-facing `*_TOOL` description and the UI `TOOL_CATALOG` description diverge in a way the registry can't losslessly hold (likely OPEN — verify per tool). External-MCP capability discovery stays OUT (tracker line 171 open question, deferred). Docs-only; repo clean. |
| Definitive session pathway | active | `refactor plan/definitive-session-pathway.md` | Refactor execution control | Roadmap, foundation specs, slice 001 implementation | Created to remove discretionary next-step choices. It fixes the required order as: close slice 001, plan/build/verify slice 002 project live outbox, then work items, workflows, agents, conversation send/replay, mailbox, Channel cutover, runtime split, MCP typed client, and compatibility cleanup. |
| Bundled pinned Claude CLI (distribution) | built (Windows) — pending packaged-app verification | `refactor plan/build-slices/distribution-bundled-claude-cli.md` | `packages/runtime` (claude-resolver), `apps/server` + `packages/agent-host` (boot wiring), `apps/desktop` (electron-builder packaging) | Section 10 onboarding/distribution; independent of the numbered refactor sequence | **BUILT 2026-06-02.** Ships a PINNED Claude Code CLI INSIDE the app, used by default, ISOLATED from the user's global install — so a CLI auto-update can't break Caisson and the user can run their own `claude` independently. Resolver (`claude-resolver.ts`) gained a `bundled` candidate at priority #4 (after explicit override/`claudeExe`/`CLAUDE_EXE`, before PATH/`~/.local/bin`; existence-gated) + `setBundledClaudeExe()`. Server boot + agent-host boot register it from `PC_BUNDLED_CLAUDE_EXE`; desktop sets that env to `<resources>/claude/claude.exe` before the in-process server boots. Packaging: `stage-claude.mjs` (chained into `prepackage`) copies the pinned binary from `~/.local/bin`/`PC_CLAUDE_SRC` into `staging/claude/`, **asserts version == 2.1.160** (build fails on mismatch) + writes a `VERSION` marker; new `extraResources` entry ships it. **Decisions: vendoring = native single-file binary** (2.1.160 ships as a ~238 MB native `claude.exe`, NOT npm); pin = 2.1.160; auth = share `~/.claude` (no `CLAUDE_CONFIG_DIR` set — only the binary is isolated); precedence = explicit hatches win, bundled beats PATH. Gates GREEN: `@pc/runtime` test +8 / full `pnpm typecheck` exit 0 / `@pc/agent-host` build / `stage:claude` verified against the real binary + pin-mismatch rejection confirmed. LEFT: packaged-app launch verification (human — `dist:dir` + run + dispatch); cross-platform binaries (Windows-only wired; mac/Linux need their `claude` via `PC_CLAUDE_SRC`); optional settings-UI version surface. |

## Change Log

| Date | Change |
|---|---|
| 2026-06-02 | **BUILT Bundled pinned Claude CLI (distribution)** (Windows; pending packaged-app verification). Resolver `bundled` candidate (priority #4, existence-gated) + `setBundledClaudeExe()`; server + agent-host boot register `PC_BUNDLED_CLAUDE_EXE`; desktop sets it to `<resources>/claude/claude.exe`; `stage-claude.mjs` vendors the native binary into `staging/claude/` with a hard 2.1.160 pin assert + `VERSION` marker, shipped via a new `extraResources` entry. Vendoring decision settled by evidence: 2.1.160 is a ~238 MB native single-file `claude.exe` (not an npm package), so we ship the binary. Auth shares `~/.claude` (binary-only isolation). Gates green (runtime +8 tests, full typecheck, agent-host build, staging script verified + pin-mismatch rejection). Left: packaged-app launch check (human), cross-platform binaries, optional settings-UI version surface. Files: `packages/runtime/src/claude-resolver.ts` + `index.ts`, `apps/server/src/index.ts`, `packages/agent-host/src/cli.ts`, `apps/desktop/src/main.ts` + `scripts/stage-claude.mjs` + `package.json`, `packages/runtime/test/claude-resolver.test.ts`. |
| 2026-06-02 | Added backlog item **Bundled pinned Claude CLI (distribution)** (`build-slices/distribution-bundled-claude-cli.md`, backlog — not yet planned). Captured from user direction: ship a PINNED Claude Code CLI INSIDE the Caisson app, used by default, ISOLATED from the user's global install — so a global CLI auto-update can't break PC's boot/queue/JSONL/system-prompt coupling, and the user can run their own newer `claude` with zero app impact (and vice versa). Root: `claude-resolver.ts` has no bundled candidate (override → claudeExe → CLAUDE_EXE → PATH → ~/.local/bin; lands on the global 2.1.160). Direction = add a `bundled` resolver candidate above PATH/homedir (explicit override/env still win), pin to PC's verified version, vendor via electron-builder `extraResources`/`asarUnpack`. **Distribution/onboarding track (Section 10), NOT in the numbered refactor sequence — deliberately not numbered 020–023 (reserved by slice 019 for the contract-first arc).** Open decisions flagged STOP-and-confirm (pin version, vendoring mechanism, auth-dir share-vs-isolate, resolver precedence). No code changed; promote to a full code-grounded slice plan when distribution is prioritized. |
| 2026-06-01 | Added **slice 016 — MCP Capability Registry Unification** (`build-slices/016-mcp-capability-registry-unification.md`, planned). Finishes what slice 011 started: collapse the THREE hand-maintained pc-rig tool sources (`TOOLS`, `TOOL_CATALOG`, `capabilities.ts`) + the derived `PC_RIG_TOOL_NAMES` into ONE canonical parity-guarded `PC_RIG_TOOL_REGISTRY` in `@pc/domain` (registry home decided = domain, not contracts), with inputSchema moved IN so the registry is the whole agent wire and `@pc/mcp` keeps only a name→handler map; every view derives in registry order; a bijection parity test makes "tool added to one list, missing from the others" structurally impossible. Motivated by the verified `TOOL_CATALOG` drift (`pc_list_areas` missing until slice 010). WIRE FROZEN, no new emit path (015c gate stays green), no DB migration (code-not-DB locked). **Execution order: slice 016 runs BEFORE 013/014** (user in-session direction). Resolves the tracker subsystem open question "where should the canonical capability registry live?" → `@pc/domain`. External-MCP capability discovery stays out (deferred). |
| 2026-05-30 | Incorporated `target-architecture.md` as the north-star architecture for subsystem analysis and holistic synthesis. |
| 2026-05-31 | Added **slice 015 — State Propagation Spine** (`build-slices/015-state-propagation-spine.md`, planned). Promote `live_outbox` to the one canonical door; build the missing RELAY + the ADR WS `lastVersion` cursor handshake so a blipped/reconnecting tab never misses an update; migrate ALL DB-owned-fact bypassers (mailbox, "Waiting on you", orchestrator snapshots, etc.) onto it; enforce a static no-bypass gate (pass-through exempt: PTY output, chunk/jsonl spam, file-tailed transcript). Sub-slices 015a/b/c; **015c subsumes the unwritten slice 012**. Recommended order: 010 → 015a → 015b → 015c → 011. This is the structural fix for the priority-#1 "UI refresh / WebSocket / event propagation" staleness — root cause confirmed = no relay + fanout-only WS hub + reconnect-replay wired for `project.changed` only. |
| 2026-05-31 | Completed Session 34 / slice 009 review → **IMPLEMENTED**. Live-tracing OBJ-2 exposed it as unreachable (pause gate read the stale in-memory handle, not the DB row → `pc_ask_user` rejected `409 wrong-state` forever). Added + built **OBJ-2A**: pause/resume gates read the reconciled DB row + on-demand host level-read; sweep/event re-seed the registered host handle; factory init-order race closed (handle demoted from state authority, per the state-propagation ADR). No DB migration / no contract change; full typecheck + `@pc/server` 81/81 green; live-verified on the UI (pause→answer→resume→completed). Next: Session 35 plan slice 010 (Areas). |
| 2026-05-31 | Completed manual Session 32 / slice 009 planning. Created `refactor plan/build-slices/009-runtime-host-transient-worktrees.md` and marked the build-slice plan `planned`. REPRIORITIZED around three confirmed runtime-host defects (broader runtime-host/transient-worktree split scoped down, rest deferred to slice 011): OBJ-1 (LEAD) agent→mailbox boot-order gate-wiring (construct mailbox bindings before the boot-reattach/reconcile-sweep/liveness-sweep handlers — mechanism (a), TDZ-justified — and thread `deliveryRouter`+`mailboxEnqueue` through them; the boot deps already accept the params); OBJ-2 host-backed resume-send (typed `not-resumable` host response + server `resume-failed` mapping; exact branch needs a live host trace); OBJ-3 `send-protocol.ts` echo-timeout composer recovery (clear the composer, never blind-submit). No DB migration; no contract change expected. No implementation code changed. Next session is Session 33: build slice 009. |
| 2026-05-30 | Marked priority refactor sequence: UI refresh/WebSocket, chat, agents, workflows, MCP/tooling, channel-server replacement, then whole-system synthesis. |
| 2026-05-30 | Created tracker and subsystem handoff prompt. |
| 2026-05-30 | Documented chat runtime and transcript UI subsystem; marked it `needs synthesis` because transcript storage, live envelope replay, pending asks, and agent transcript convergence cross subsystem boundaries. |
| 2026-05-30 | Added agent runs subsystem handoff at `refactor plan/refactor plan docs/agents-and-agent-runs.md`; marked tracker row `needs synthesis` because it conflicts with WebSocket/live-event, mailbox, chat transcript, workflow, and MCP contract decisions. |
| 2026-05-30 | Documented WebSocket/event propagation handoff and marked it `needs synthesis`; added live event contracts/outbox as a discovered subsystem candidate. |
| 2026-05-30 | Documented MCP/tooling handoff and marked it `needs synthesis` because tool contracts, capability registry ownership, runtime MCP readiness, external MCP server validation, workflows, agents, live events, and mailbox ownership need cross-subsystem reconciliation. |
| 2026-05-30 | Documented Channel server replacement / agent messaging inbox handoff and marked it `needs synthesis`; added human review inbox and approval surfaces as a discovered subsystem candidate. |
| 2026-05-30 | Documented workflows and workflow builder handoff and marked it `needs synthesis` because workflow identity, live events, active-run recovery/cancellation, review inbox, agents, MCP, and transient builder state cross subsystem boundaries. |
| 2026-05-30 | Updated Channel replacement target to no-channel architecture: durable mailbox plus UI inbox and app-injected orchestrator turns; Channel/dev-channel bridge is a delete target, not a compatibility adapter. |
| 2026-05-30 | Created holistic architecture synthesis at `refactor plan/holistic-architecture-synthesis.md`; marked six priority subsystem docs as `synthesized`; added follow-up candidates for shared contracts/app-services, transcript store, mailbox workers, capability registry, and pending interactions/approvals. |
| 2026-05-30 | Added post-synthesis planning-to-build workflow, foundation spec queue, build-readiness gates, and planning artifact tracker so new sessions can proceed toward implementation coherently. |
| 2026-05-30 | Updated `AGENTS.md` for the post-synthesis planning-to-build workflow and added README guidance for `foundation specs/`, `build-slices/`, and subsystem docs. |
| 2026-05-30 | Recorded refactor target decision: build the new architecture inside the current repo; use separate scratch directories only for disposable spikes, not the real implementation. |
| 2026-05-30 | Added manual session tracker at `refactor plan/refactor-session-tracker.md` and linked it from `AGENTS.md` and the tracker startup workflow. |
| 2026-05-30 | Created implementation roadmap at `refactor plan/implementation-roadmap.md`; marked roadmap artifact `planned`; named first foundation vertical slice and confirmed shared contracts/app services spec as the next planning artifact. |
| 2026-05-30 | Created shared contracts and app services foundation spec at `refactor plan/foundation specs/shared-contracts-and-app-services.md`; marked the artifact and subsystem `planned`; selected project list/project metadata as the first contract family and typed localhost HTTP as the first MCP adapter path. |
| 2026-05-30 | Created live events and outbox foundation spec at `refactor plan/foundation specs/live-events-and-outbox.md`; marked the artifact and subsystem `planned`; defined canonical `LiveEvent` scope/cursor semantics, proposed `live_outbox`, preserved legacy WS adapters during migration, and selected `project.changed` as the first event family. |
| 2026-05-30 | Created mailbox and pending interactions foundation spec at `refactor plan/foundation specs/mailbox-and-pending-interactions.md`; marked the artifact plus mailbox and pending-interaction subsystem rows `planned`; defined typed recipient addresses, mailbox delivery state, pending-interaction action ownership, UI inbox/orchestrator-turn policy, and send-queue acceptance as the first runtime-turn acknowledgement. |
| 2026-05-30 | Created runtime transcript and conversation store foundation spec at `refactor plan/foundation specs/runtime-transcript-and-conversation-store.md`; marked the artifact and conversation/runtime transcript store subsystem `planned`; defined file-backed transcript repository first, SQLite mirror-only storage until parity tests pass, transcript `seq` replay cursor versus live outbox cursor, send queue service boundary, `/api/ask` compatibility, and mailbox-facing `enqueueRuntimeTurn`. |
| 2026-05-30 | Created work items/stages/fields/attachments handoff at `refactor plan/refactor plan docs/work-items-stages-fields-attachments.md`; marked the artifact plus work-item and attachment subsystem rows `planned`; documented agent/workflow/MCP/live-event dependencies, event gaps, stage/field risks, attachment policy gaps, and characterization-test needs. |
| 2026-05-30 | Created Phase 0 test characterization plan at `refactor plan/phase-0-test-characterization-plan.md`; marked the artifact `planned`; documented the missing active test harness, 155 deleted tracked tests as restore candidates, P0/P1/P2 characterization coverage by subsystem, known-gap policy, local/CI commands, and slice-specific test gates. |
| 2026-05-30 | Created first foundation build-slice plan at `refactor plan/build-slices/001-foundation-vertical-slice.md`; marked the artifact `planned`; selected project list/project metadata as the smallest contract-first slice and limited implementation scope to shared contracts, a narrow project service seam, route parity, a non-durable `project.changed` refetch event, web contract/refetch integration, and focused tests. |
| 2026-05-30 | Prepared manual Session 9 by syncing `refactor-session-tracker.md` through Session 8, clarifying that Session 9 is the first build slice rather than roadmap Phase 9, updating startup workflow when no planning artifact is in progress, and standardizing the first project live/refetch event name to `project.changed`. |
| 2026-05-30 | Completed manual Session 9 / build slice 001. Restored minimal focused tests, added project shared contracts and app-service seam, adapted project routes to preserve wire compatibility and publish a non-durable `project.changed` refetch hint, updated the web project client/types and WS filtering/refetch handling, and verified with scoped tests/typechecks plus full `pnpm typecheck`. |
| 2026-05-30 | Fixed follow-up project create blocker from Session 9 verification: attach-to-git scaffold now writes a tracked `.project-companion/setup-wizard-prompt.md` file before the scaffold commit and accepts empty partial `.project-companion/` directories from the previous failed attempt. Added `apps/server/test/project-create.test.ts`; verified with targeted project-create test, `@pc/server` test, and `@pc/server` typecheck. |
| 2026-05-30 | Added clean-worktree requirement for future sessions: start by checking `git status --short`, resolve dirty state before new work, commit completed work or explicitly stash deferred work before handoff, and end with a clean status. |
| 2026-05-30 | Ran follow-up two-client live-event verification. Fixed background project websocket target stability so metadata-only refetches no longer reconnect sockets and miss back-to-back `project.changed` events. Reversible project rename/restore now propagates across two clients without refresh; final create/archive two-client verification is the Session 10 closeout gate. |
| 2026-05-30 | Added `refactor plan/definitive-session-pathway.md` and expanded the session tracker through slice 011 so future sessions follow a fixed plan/build/verify pathway instead of choosing ad hoc next steps. |
| 2026-05-30 | Closed manual Session 10 / slice 001 verification. Re-ran the slice 001 automated gates and full repo typecheck, then verified two isolated browser clients for project create visibility, metadata rename/update, name restore, archive/delete visibility, and scaffold cleanup on the running dev stack. Slice 001 is fully verified; the next slice is fixed as `refactor plan/build-slices/002-project-live-outbox.md` planning only. |
| 2026-05-30 | Completed manual Session 11 / slice 002 planning. Created `refactor plan/build-slices/002-project-live-outbox.md`; marked the build-slice plan `planned`; fixed the scope to durable `live_outbox` for `project.changed` only with contracts, DB migration/repo, publisher boundary, replay route, websocket compatibility fanout, web cursor/refetch behavior, tests, rollback, and stop conditions. |
| 2026-05-30 | Completed manual Session 12 / build slice 002. Implemented canonical `project.changed` live-event contracts, additive `live_outbox` schema/migration/repo, atomic project mutation plus outbox writes, `/api/live-events` replay, canonical plus legacy fanout, and web cursor/replay/refetch handling. Automated gates passed; browser verification against the already-running dev stack is deferred to Session 13 because the current API process had not loaded the new replay route and was not restarted. |
| 2026-05-30 | Completed manual Session 23 / slice 006 planning. Created `refactor plan/build-slices/006-conversation-send-replay-service.md` and marked the build-slice plan `planned`. Scope is a read + command-facade seam over the existing orchestrator chat runtime: contracts mirroring the exact session/transcript/send-queue/hook-ask wire, a file-backed `TranscriptRepository` + `ConversationReplayService` over the on-disk JSONL (byte-identical responses + additive `?afterSeq=` read on the transcript `seq` cursor), a `ConversationSendService` facade over the existing send queue with a mailbox-safe `enqueueRuntimeTurn`, and a durable `pending_interactions` shadow around the unchanged in-memory `/api/ask` resolver. Honors JSONL-canonical: the high-frequency `jsonl` stream stays transient and off `live_outbox`. DB: at most one additive `pending_interactions` table (the only possible migration; flagged STOP-and-confirm vs deferring the shadow to slice 007). Mailbox/Channel/runtime-host (incl. deferred host-resume defect)/MCP/cleanup out of scope. |
| 2026-05-30 | Completed manual Session 14 / slice 003 planning. Created `refactor plan/build-slices/003-work-items-stages-fields-events.md` and marked the build-slice plan `planned`. Scope is work-item/stage/field/attachment shared contracts, compatibility row<->DTO adapters, a server-owned mutation gateway consolidating the bypassing agent-verification/auto-advance/DAG-move/legacy fields-only mutation paths, and durable `work-item.changed` / `stage.list.changed` live events on the reused slice-002 `live_outbox` (with `field-schema.list.changed` / `attachment.changed` payload contracts and legacy websocket projections). No implementation code changed; no new DB migration. |
| 2026-05-31 | Completed manual Session 30 / build slice 008 (built-but-not-yet-human-reviewed). Routed the three delivery flows through the slice-007 mailbox behind three independent per-flow env gates (`PC_DELIVERY_AGENT`/`PC_DELIVERY_WORKFLOW_REVIEW`/`PC_DELIVERY_WEBHOOK`, `channel`\|`mailbox`), each defaulting to `channel` (unknown/missing ⟹ `channel`) so a fresh checkout is byte-identical to today; the gate selects ONE path (no double delivery). NEW `delivery-routing.ts` selector. Flow A: gated `deliverAgentEnvelope` helper threaded through agent-run routes → factory/terminal/pause (`orchestrator-session` + `orchestrator-turn`, keys `agent:${runId}:${eventKind}` / `agent-ask:${pendingAskId}`). Flow B: a `WorkflowReviewDelivery` seam threaded from index.ts → ProjectRuntime → DagRunServiceOptions (one injected fn, no runtime-host refactor; `active-orchestrator` + `orchestrator-turn`, key `workflow-review:${runId}:${nodeId}`); slice-004 fact + legacy broadcast in BOTH positions. Flow C: a `webhookSink` seam on ChannelServer (durable `project-inbox` + `ui-inbox`, no silent drop; `channel-event` broadcast in BOTH positions; bridge fan-out skipped only on mailbox). Boot-order friction resolved by keeping recovery-path emitters on Channel (no mailbox port wired). No DB migration; Channel/`/channel/*`/`enqueueAndPush`/`postChannel`/`pending_asks`/`compat-channel` NOT deleted. 4 new test files; gates green (contracts 60, db 20×3, app-services 60×3, server 59×3 deterministic, web 19, full typecheck exit 0, diff-check clean). Human review pending (Session 31). |
| 2026-05-31 | Completed manual Session 29 / slice 008 planning. Created `refactor plan/build-slices/008-channel-cutover.md` and marked the build-slice plan `planned`. Scope is the gated, fallback-preserving cutover of the three delivery flows (agent delivery, workflow orchestrator-review, external webhook) from Channel onto the slice-007 mailbox: a per-flow delivery selector (`PC_DELIVERY_AGENT`/`PC_DELIVERY_WORKFLOW_REVIEW`/`PC_DELIVERY_WEBHOOK`, each defaulting to `channel`), each call site re-pointed at `MailboxService.enqueue` (existing platform; no new primitive) with stable idempotency keys, and Channel kept fully in place as the reversible fallback. No DB migration. Channel deletion stays in slice 011. Verified-from-code wiring friction flagged (Flow-B `postChannel` composed in `ProjectRuntime`; per-CC bridge receives a runtime turn under mailbox; agent boot-sweep emitters predate the mailbox singleton). Runtime-host split (009), MCP (010), Channel deletion (011), `pending_asks` convergence, and the slice-004 boot-reconcile gap are out of scope. No implementation code changed. |
| 2026-05-30 | Completed manual Session 26 / slice 007 planning. Created `refactor plan/build-slices/007-mailbox-platform.md` and marked the build-slice plan `planned`. Scope is the durable mailbox PLATFORM (additive, pre-cutover): mailbox/pending-interaction contracts (message/recipient/delivery DTOs + typed `MailboxAddress` union + canonical `mailbox.*`/`pending-interaction.changed` payloads on the slice-002 envelope); the FIRST real schema migration of the refactor — an additive CREATE-only `0036_*` adding `pending_interactions` + five `mailbox_*` tables with explicit Drizzle ledger-lies / fresh-DB-boot safety (mirrors `0035_live_outbox.sql`, `assertSchemaIntact` fresh-DB test); `MailboxService` + `PendingInteractionService` single-transaction outbox-write-doors; a lease/ack/retry/dead-letter delivery worker whose `orchestrator-turn` channel rides the slice-006 `enqueueRuntimeTurn` facade (never raw-send/Channel); the DEFERRED slice-006 `/api/ask` ask-shadow as the first `pending_interactions` writer (in-memory resolver stays the authority); and a UI inbox surface + `/api/live-events` extension. The Channel→mailbox cutover (slice 008, Channel stays in place), agent `pending_asks` convergence, runtime-host split incl. host-resume defect (009), MCP (010), and cleanup (011) are out of scope. No implementation code changed. |
| 2026-06-01 | Completed manual Session 39 / build slice 011 (built-but-not-yet-human-reviewed). Routed `@pc/mcp` tool internals through existing `@pc/contracts` DTOs behind a typed localhost client + a derived parity-guarded capability registry — WIRE FROZEN (tool names / ListTools order / input schemas / HTTP payloads+paths / agent-visible result strings byte-identical, proven by per-family golden tests written FIRST + the 11G spawned-bundle ListTools-order smoke). 6 commits `3ae102cc`..`8c90ac6a`: foundation (`@pc/contracts` dep + `test` script + 11A `CAPABILITIES` registry with full TOOLS↔registry bijection test + 11B `TypedLocalhostClient` over injected raw `ToolContext` helpers, parsed DTO + typed error + raw `{status,body}` fallback, exposed as `ctx.client`); 11C work-item/project-config; 11D workflow (fire/get); 11E agent-run/pending (invoke/continue/ask-trio/answer). LEFT RAW + noted (no covering DTO; STOP-and-confirm not triggered): all pod-CRUD in agents.ts, list/inspect/kill runs, workflow publish/create/update/delete/draft/list/complete/node-failed, claude-md/replace-stages/replace-field-schemas. 11F hardened `parsePodMcpServerConfig` shape validation (no valid stdio/http config changed; discovery OUT) + rewriter suffix-match tests. NO new emit path (no `broadcast`/`live_outbox`/`@pc/db` in `packages/mcp` → 015c no-bypass gate stays green); NO DB migration; NO contract/route/app-service behavior change; NO new `@pc/contracts` parser. Gates green: contracts test 68, `@pc/mcp` typecheck 0 / build 690.3kb / test 46, `@pc/server` test 121 / typecheck 0, `@pc/web` typecheck 0, full `pnpm typecheck` 0, `git diff --check` 0. Dev stack not restarted; repo clean. Human review pending (Session 40). |

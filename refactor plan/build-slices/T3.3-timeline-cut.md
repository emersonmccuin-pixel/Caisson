# T3.3 — The cut: stop dual-carrying live-event frames on the chat timeline

Theme 3, final slice of the 015→018 "one client door" arc. Branch `refactor/auto-pathway`.

## Goal
After T3.3 a relay `live-event` frame reaches the UI ONLY through the identity-keyed
live store (`live-store.ts`) — never the chat timeline. Early-`return` the frame in the
WS handler before `dispatchSession`, delete the reducer's live-event admission + the
cross-session-reset retention of those frames, and drop the vestigial `_events` param.

## ⛔ BLOCKED — safety precondition NOT met (must run a migration slice first)
The cut is safe ONLY when no consumer reads live-event frames off `events[]`. It is NOT
safe today. T3.1/T3.2 migrated the 4 stragglers + the resource-list family; the 018
"migrate every view" step was never completed. **~12 live-event-via-timeline reads remain.**
Migrate them to the store in a prerequisite slice (T3.2b) BEFORE this slice runs.

### Still reading live-event frames off the chat timeline (`wsEvents` = `materializeChatSessionEvents`)
| File:line | Entity | Today's effect | Migrate to |
|---|---|---|---|
| `KanbanBoard.tsx:181-190` | `area` (deleted) | refetch work items | `useLiveEntitySignature('area', projectId)` → refetch |
| `WorkItemsTable.tsx:90-100` | `area` (deleted) | refetch | same |
| `WorkItemDetailModal.tsx:150-159` | `field-schema` | setFieldSchemas | `useLiveEvents('field-schema', projectId)` → latest by version |
| `WorkItemDetailModal.tsx:1146-1156` | `work-item` | activity-history rows | `useLiveEvents('work-item', projectId)` filter by id |
| `InitiativeInspector.tsx:371-377` | `work-item` | refetch children | `useLiveEntitySignature('work-item', projectId)` → refetch |
| `InitiativeInspector.tsx:764-777` | `work-item` | activity-history rows | `useLiveEvents('work-item', projectId)` filter by id |
| `ChatWorkItemModalMount.tsx:61-71` | `work-item` | refetch open item | `useLiveEntitySignature('work-item', projectId)` → refetch |
| `Orchestrator.tsx:310-316` | `session-title` | rename session header | `useLiveEvents('session-title', projectId)` → latest payload |
| `AgentsList.tsx:407-414` | `pod` | reload pod bundle | `useLiveEvents('pod', projectId)` filter by `payload.podId === pod.id` |
| `ProjectSettingsPanel.tsx:343-352` | `project-claude-md` | re-probe status | `useLiveEntitySignature('project-claude-md', projectId)` → re-probe |
| `SetupWizardModal.tsx:85-90` | `project-claude-md` | auto-close | same (KEEP array for `setup-wizard-*` transient envelopes) |
| `WorkflowBuilderModal.tsx:162-174` | `workflow-definition` | auto-close | `useLiveEvents('workflow-definition', projectId)` (KEEP array for `workflow-builder-*` transient envelopes) |

NOT affected (verified): `useStatuslineSync` (`statusline-snapshot` is a chat-channel envelope,
not a relay frame), and all already-migrated consumers (App project-list, use-all-projects-ws,
SessionsRail, use-mailbox-inbox, use-rich-link-invalidator, HostHealthPill, useResourceList family).

> **Run order:** ship **T3.2b** (migrate the 12 above, reconcile-first, live-verify each on the
> 018 bar) → THEN T3.3. Do not start the cut steps below until the table is empty.

---

## Current behavior — the double-carry (file:line)
1. **Store feed (keep):** `use-project-ws.ts:280-290` — on `live-event`, advance cursor + `applyEnvelope(env)`.
2. **Fall-through (the double-carry):** the `live-event` block does NOT `return`; control reaches
   `const final = env` → `dispatchSession({type:'envelope', env: final})` (~:311/:331).
3. **Reducer admission:** `chat-session-reducer.ts:194-207` scope guard explicitly ADMITS a
   live-event frame (`:203-204`); → default case → `applyDelta` → `appendUnsequenced` → lands in
   `unsequenced` + `timeline`.
4. **Cross-reset retention:** `applySessionChanged` (~:397) → `preserveProjectEventsForSessionReset`
   (~:501) → `shouldPreserveProjectEventAcrossSessionReset` (~:536-551) returns `true` for a
   live-event frame (deny-list of chat types) → it survives a new-session reset.
5. **Vestigial `_events`:** `use-resource-list.ts:60 _events: WsEnvelope[]` (never read; records come
   from `useLiveEvents`); forwarded by `use-project-stages.ts:25`, `use-project-workflows.ts:26`,
   `use-project-areas.ts:21`.

## Step-by-step (T3.3 proper — AFTER T3.2b)
### 1. WS handler — early return on live-event
`use-project-ws.ts` message listener: after `applyEnvelope(env)` (~:289) add `return;`. Verify the
other branches are independent: `server-pong` returns before the block; `live-reset` has its own
block + return; `event`/`session-changed`/`session-replay` are handled after — none reachable from a
`live-event` frame, so the early return cannot strand them.
### 2. Reducer — delete the live-event admission
`chat-session-reducer.ts` scope guard (~:199-204): delete the `isLiveEventFrame` clause (a leaked
frame is now correctly REJECTED, not admitted). Remove the `isLiveEventFrame` import IF unused
(keep `isProjectChangedLiveEventFrame` from `@/features/projects/live-events` — different concern).
### 3. Reducer — delete/limit the cross-reset retention
`shouldPreserveProjectEventAcrossSessionReset` (~:536-551). Also called by
`shouldPreserveUnsequencedAcrossSnapshot` (~:533) — do NOT delete blind. Make its allow-set EXPLICIT
(return `true` only for the envelopes that must survive — `session-changed`/`state` per the data)
rather than the current deny-list-allow-everything-else.
### 4. Drop the vestigial `_events` param
`use-resource-list.ts:60`; `use-project-stages.ts:25`, `use-project-workflows.ts:26`,
`use-project-areas.ts:21`; check `use-project-pods.ts`. Update call sites (grep the hook names +
`useResourceList(`).

## What stays
Store feed + cursor advance (sole live path); all chat envelopes (`event`, `jsonl`,
`session-changed`, `session-replay`, `send-ack`, `send-queue-snapshot`, `runtime-state`, `raw`,
terminal); `live-reset` handling; `isProjectChangedLiveEventFrame`/refetch-envelope gating;
`scanProjectChangedEvents` + `live-events.ts` guards (T3.4 territory); `materializeChatSessionEvents`
shape (just smaller).

## Tests
- New `reducer-no-live-event-timeline.test.ts`: a `live-event` envelope into the reducer → timeline
  length does NOT grow + `materializeChatSessionEvents` contains no live-event frame; a chat envelope
  after → timeline DOES grow.
- New-session reset: no live-event frame survives; chat retention/`session-changed` re-append unchanged.
- One migrated consumer asserts it updates from the store with an EMPTY `events[]` (zero timeline dep).
- Re-grep `apps/web/test/` for any test asserting a live-event frame in the timeline; update/remove.

## Gates
`pnpm typecheck`; `pnpm --filter @pc/web test`; `git diff --check` — **web-only expected** (flag any
server/package touch — T3.3 is pure client deletion).

## Live-verify (018 bar, post-cut — gated reload only if T3.2b shipped a server change; T3.3 client-only)
During a live active session, NO refresh: mailbox, rich-link, sessions rail + Orchestrator header,
project list, work-item Kanban/Table/Inspector/open-modal, area delete, pod change, CLAUDE.md change,
workflow-definition builder-close — all update live; reducer timeline does NOT grow on relay frames;
no render storm.

## Open questions / risks
- **R1 (blocker):** T3.2b must land + be live-verified first. Miss one consumer → it goes stale
  silently during active sessions (won't show on a calm reload).
- **R2:** `shouldPreserveProjectEventAcrossSessionReset` is shared with the snapshot-preserve path —
  make the allow-set explicit, don't delete blind.
- **R3:** verify `use-project-pods.ts`'s `events` usage before dropping its param.
- **R4 (collision):** `chat-session-reducer.ts` is hot — stage selectively.

## Critical files
- `apps/web/src/hooks/use-project-ws.ts`
- `apps/web/src/hooks/chat-session-reducer.ts`
- `apps/web/src/hooks/use-resource-list.ts`
- `apps/web/src/components/Shell.tsx` (the `wsEvents` fan-out to the 12 consumers)

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
out/
coverage/
apps/desktop/release/
apps/desktop/staging/

# Refactor pipeline gate logs (transient tooling output)
.gatelogs/

# Runtime state (transient)
data/*
!data/.gitkeep
apps/server/data/
*.db
*.db-*
*.sqlite
*.sqlite-*

# Manual / accidental data-dir snapshots (timestamped)
data.bak.*

# Worktrees managed by the runtime (regenerated)
worktrees/

# Dogfood scaffold: the app writes .project-companion/ into managed projects.
# At this repo's own root it is an incidental dogfooding artifact, not tracked.
.project-companion/

# Dev fixture: workspace/ has its own embedded git repo (per-project shape).
# Default project templates will land under templates/ when the chassis work needs them.
workspace/

# Local agent/browser tool state
/.playwright-mcp/
/.mcp.json
/.claude/

# Playwright artifacts (regenerated per run)
test-results/
playwright-report/
tests/playwright/results.json
tests/playwright/snap-shell.png
tests/playwright/.tmp-*.spec.ts
tests/playwright/readme-*.spec.ts
# apps/web Playwright evidence + reports
apps/web/e2e/evidence/
apps/web/e2e/report/

# Logs and scratch files
*.log
*.log.*
*.jsonl
*.out
.tmp-*
.dev-logs/
report.*.json
*.tmp
*.tmp.*
*.bak
*.orig
.tr-deliverable.tmp.md
orch-patch.json
post-chat.yml
/E*Claude

# Local env and signing material
.env
.env.*
!.env.example
*.p12
*.pfx
*.pem
*.key
*.cer
*.crt
*.mobileprovision

# Debug artifacts (pw-debug screenshots, visual QA dumps, local mockups)
scripts/*.png
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.html
Pics/

# Local experiment outputs. Promote durable fixtures into tests/ or docs/ before tracking.
/labs/**/node_modules/
/labs/**/reports/
/labs/**/sandbox/

# Archived planning / scratch (local-only; kept out of the repo)
archive/

# Secrets / keys (never commit)
*.p8
cia_next_agent.*

# OS / editor noise
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
.vscode/
.idea/
*.swp
.obsidian/

# Session scratch (diagnostic dumps + throwaway probe scripts — never history)
r2r_raw.txt
scripts/_*.ps1
apps/web/e2e/qa-scratch/

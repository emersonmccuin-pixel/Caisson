# Dev controls/diagnostics
Not sure what we need on these anymore. I am curious how this would look in the new build since we're not doing separate dev and prod builds? Like can the elecetron app hae a dev build that I use for rapid testing/iterating?

# onboarding
the check for claude code binary should be the exact pinned version. that isn't clear to me that it is. 
Let's ditch the setup wizard. too complicated and not necessary
Make sure project files are gitignored automatically?

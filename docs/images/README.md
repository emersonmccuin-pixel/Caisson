# README screenshots

The main README references these images. Drop PNGs here with these exact
filenames and they'll render on GitHub.

**Capture from a realistic, non-developer project — not Caisson's own dev board.**
The current shots show `PC-PTY-CHAT-### [BUG]` cards and "spin up a caisson
agent" chat history. That reads as a tool for building Caisson. Stage a project
that looks like normal knowledge work instead — the **sales follow-up** or **HR
onboarding** examples from the README are good candidates. Real-looking content,
populated state (empty states make weak screenshots), OS chrome trimmed.

| File | Shot | What it should show |
| --- | --- | --- |
| `01-orchestrator-chat.png` | The project chat — hero shot. | A real exchange with the AI Project Manager about everyday work (e.g. "draft the follow-up for this morning's call"), not a dev/agent-testing session. |
| `02-board.png` | The kanban board with work items across stages. | Cards that read like normal tasks ("Follow up — Acme call", "Onboard J. Rivera"), not bug tickets. |
| `03-workflow-builder.png` | A workflow open in the Workflows tab — trigger, description, step graph. (Or the build conversation mid-interview.) | A workflow a non-dev would build (sales follow-up, onboarding). |
| `04-specialists.png` | The roster of specialists (Agents tab). | The stock pods. Fine as-is; recapture only if it shows dev-specific custom agents. |
| `05-work-item.png` | A single work item in the inspector (brief + children/documents/activity). | A realistic task, not a Caisson bug. |
| `06-workflow-graph.png` | A workflow visualized as a left-to-right graph. | Same realistic workflow as `03`. |

Suggested width: ~1600px, PNG. Crop to the content.

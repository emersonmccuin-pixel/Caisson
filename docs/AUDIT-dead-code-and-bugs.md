# App Audit — Dead Code, Misconfigured Subsystems & Bugs

_Generated 2026-06-02 · branch `refactor/auto-pathway` · multi-agent audit (12 subsystem auditors + per-finding adversarial verifier)_

## Summary

Typecheck (`pnpm -r typecheck`) passes clean, so this is a **logic / dead-code** audit, not a type audit.

**53 confirmed findings** (55 candidates → 53 survived adversarial verification, 2 refuted).

| | Count |
|---|---|
| Dead code | 43 |
| Misconfigured subsystem | 8 |
| Bug | 2 |
| **Total** | **53** |

Severity: **0 high · 13 medium · 40 low**. No high-severity issues — nothing here crashes the app or loses data. The medium items are real feature gaps and drift; the low items are mostly orphaned/unused code to delete.

### How to read this
- **Dead code** = safe to delete; no production caller (verified repo-wide incl. tests, barrels, string/dynamic refs, web mirrors).
- **Misconfig** = two things that must stay in sync but drifted, or a wired-wrong subsystem.
- **Bug** = wrong runtime behavior.

---

## Bugs (2)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | Pod-revision drift detection is wrong for project-scoped pods on resume + continuation (scope mismatch with dispatch) | `apps/server/src/services/pause-resume.ts` | lookupPodScope() always returns null, so answerPendingAsk and continueAgent compute computePodRevision({podName, projectId: null}). computePodRevision with projectId null queries WHERE agents.projectId IS NULL — i.e. it only matches the GLOBAL pod row. But the fresh-dispatch path (agent-run-factory.ts) was fixed in Section 22.1 to scope the revision to the resolved pod: computePodRevision({projectId: podPrep.podScope==='project' ? podPrep.podProjectId : null}). So when a project-scoped pod (same name shadowing a global) is dispatched: the run row stores the PROJECT pod's revision, but resume computes the GLOBAL pod's revision (or null). The two can never match, so podRevisionDrifted (surfaced in the pc_answer_pending HTTP response and used for drift detection §6.4) is a false positive / null-vs-value mismatch for every project-scoped pod. The stub's own comment admits it's only 'correct for all six stock pods (all global).' |
| 2 | medium | Tier-1 auto-verification PASS rolls up the work item to 'complete' without announcing a live event — board never updates | `apps/server/src/services/agent-verification.ts` | On a passing tier-1 auto-verification, acceptContract calls `applyRunOutcome(workItemId, 'complete', ...)` and then `autoAdvanceToDoneStage(...)`, but never calls `announceWorkItemRow`. Both `applyRunOutcome` and `moveWorkItemStage` (used by autoAdvance) are raw DB writers that bump `version` but write NO `live_outbox` row. The work-item-writer.ts contract states 'EVERY mutating write of a work_items row MUST be announced.' The terminal-effects caller only emits a Channel terminal envelope to the orchestrator mailbox — not a `work-item.changed` frame. Result: when an agent's deliverable auto-passes, the kanban/work-item views do not refresh to show the card as done or moved until a manual reload. The sibling approve path (agent-verification-review.ts:110-114) explicitly calls announceWorkItemRow after the same roll-up, proving the inconsistency. |

#### 1. Pod-revision drift detection is wrong for project-scoped pods on resume + continuation (scope mismatch with dispatch)

- **Severity:** medium · **Confidence:** high · **Area:** Server — agent run lifecycle
- **File:** `apps/server/src/services/pause-resume.ts` — lookupPodScope (L650-659), used by answerPendingAsk (L335-339) and continueAgent (L598-602)
- **Issue:** lookupPodScope() always returns null, so answerPendingAsk and continueAgent compute computePodRevision({podName, projectId: null}). computePodRevision with projectId null queries WHERE agents.projectId IS NULL — i.e. it only matches the GLOBAL pod row. But the fresh-dispatch path (agent-run-factory.ts) was fixed in Section 22.1 to scope the revision to the resolved pod: computePodRevision({projectId: podPrep.podScope==='project' ? podPrep.podProjectId : null}). So when a project-scoped pod (same name shadowing a global) is dispatched: the run row stores the PROJECT pod's revision, but resume computes the GLOBAL pod's revision (or null). The two can never match, so podRevisionDrifted (surfaced in the pc_answer_pending HTTP response and used for drift detection §6.4) is a false positive / null-vs-value mismatch for every project-scoped pod. The stub's own comment admits it's only 'correct for all six stock pods (all global).'
- **Evidence:** Read pause-resume.ts lookupPodScope returns null with a comment acknowledging the limitation. Read agent-run-factory.ts L364-367 which scopes podRevisionAtDispatch to podPrep.podScope/podProjectId. Read packages/db/src/repos/pod-revision.ts L31,L39: projectId===null ⇒ isNull(agents.projectId), so null only matches globals. The codebase actively supports project-scoped pods (PreparePodSpawn.podScope:'global'|'project', project-first resolution in getPodForSpawn per Section 22.1).
- **Verifier:** Verified by reading all cited code. CONFIRMED: lookupPodScope (pause-resume.ts:650-659) unconditionally returns null with a comment admitting it's only "correct for all six stock pods (all global)". It's called by answerPendingAsk (L335) and continueAgent (L598), both feeding computePodRevision({projectId: null}). In pod-revision.ts:39, projectId===null produces isNull(agents.projectId), matching ONLY the global pod row. The fresh-dispatch path (agent-run-factory.ts:364-367, Section 22.1 fix) scopes the revision to the resolved pod: projectId = podPrep.podScope==='project' ? podPrep.podProject…

#### 2. Tier-1 auto-verification PASS rolls up the work item to 'complete' without announcing a live event — board never updates

- **Severity:** medium · **Confidence:** high · **Area:** Server — verification & work items
- **File:** `apps/server/src/services/agent-verification.ts` — acceptContract(), lines 307-310
- **Issue:** On a passing tier-1 auto-verification, acceptContract calls `applyRunOutcome(workItemId, 'complete', ...)` and then `autoAdvanceToDoneStage(...)`, but never calls `announceWorkItemRow`. Both `applyRunOutcome` and `moveWorkItemStage` (used by autoAdvance) are raw DB writers that bump `version` but write NO `live_outbox` row. The work-item-writer.ts contract states 'EVERY mutating write of a work_items row MUST be announced.' The terminal-effects caller only emits a Channel terminal envelope to the orchestrator mailbox — not a `work-item.changed` frame. Result: when an agent's deliverable auto-passes, the kanban/work-item views do not refresh to show the card as done or moved until a manual reload. The sibling approve path (agent-verification-review.ts:110-114) explicitly calls announceWorkItemRow after the same roll-up, proving the inconsistency.
- **Evidence:** Read acceptContract (agent-verification.ts:286-319) — no announceWorkItemRow. Read applyRunOutcome (repos/work-items.ts:569-592) and moveWorkItemStage (268-297): both do `getDb().update(...).run()` with no insertLiveEvent/announce. Read agent-run-terminal-effects.ts:282-343: only emitTerminalEnvelope (mailbox), no WI announce. Contrast approveAgentWorkItem (agent-verification-review.ts:107-115) which calls announceWorkItemRow on both the advanced and updated rows.
- **Verifier:** Claim holds. Traced the full tier-1 auto-verification PASS path:

acceptContract (agent-verification.ts:307-310) calls applyRunOutcome(workItemId,'complete',...) then autoAdvanceToDoneStage(...) and returns — NO announceWorkItemRow.

Both writers are raw, non-announcing:
- applyRunOutcome (repos/work-items.ts:569-592): getDb().update().run(), bumps version, no insertLiveEvent.
- moveWorkItemStage (268-297): same — raw update, no live_outbox row.
- autoAdvanceToDoneStage (auto-advance-done.ts:34) delegates to moveWorkItemStage, so the stage move is also unannounced.

So the WI is written TWICE …

---

## Misconfigured subsystems (8)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | `ephemeral` flag advertised to orchestrator but silently dropped everywhere (deleted feature, stale 4-layer chain) | `packages/domain/src/tool-registry.ts` | The `pc_create_agent_work_item` tool schema still advertises an `ephemeral` boolean promising 'auto-archive 24h after reaching done.' The MCP handler (work-items.ts:88) forwards it, and the HTTP route (routes.ts:309) forwards `ephemeral` into the CreateAgentWorkItemInput object. But the contract-first refactor deleted the `ephemeral` work_items column (migration 0039), deleted ephemeral-work-item-sweep.ts (the only consumer), and `CreateAgentWorkItemInput`/`createAgentWorkItem` have NO `ephemeral` field. The value is accepted at three layers then silently discarded; the advertised auto-archive behavior no longer exists. The orchestrator is being told a feature works when it is fully dead. |
| 2 | medium | Contract v2 union duplicated in @pc/domain and @pc/contracts with no drift guard | `packages/contracts/src/contracts.ts` | The entire v2 contract union (the 7 deliverable mechanisms, the 13 acceptance-predicate kinds, the status/tier enums, and the is* guards) is hand-copied in packages/contracts/src/contracts.ts as a deliberate browser-bundle mirror of packages/domain/src/contract.ts (web can't import @pc/domain). The two are byte-equivalent today, but there is NO automated drift test keeping them in sync (unlike the documented pod-tool-catalog drift test at apps/server/test/pod-tool-catalog-drift.test.ts that guards the analogous TOOLS vs PC_RIG_TOOL_NAMES mirror). Add a predicate/kind to only one side and the server-side derivation/evaluation will silently diverge from what the browser DTO can represent. This is exactly the dual-source-of-truth footgun pattern, missing its guard. |
| 3 | low | renderAvailableAgents lists the orchestrator itself as a dispatchable agent | `apps/server/src/services/pod-variable-renderers.ts` | The orchestrator's {{AVAILABLE_AGENTS}} roster is rendered from listProjectVisibleAgents, which filters by isProjectDispatchable(agent) = (scope==='project' || origin==='stock'). The orchestrator pod is seeded origin:'stock' (orchestrator-pod-content.ts: name 'orchestrator', scope 'global', origin 'stock'), so it passes the filter and appears in its own dispatch list. The renderer applies no self/orchestrator exclusion. Result: the orchestrator's prompt (which embeds {{AVAILABLE_AGENTS}} at orchestrator-pod-content.ts line 122) advertises 'orchestrator' as a dispatch target, and resolveAgentForDispatch('orchestrator') in @pc/db likewise resolves it as dispatchable. The orchestrator could be told it can dispatch itself. |
| 4 | low | Stale 'eight stock specialists' comments after roster grew to nine | `apps/server/src/routes/pod-routes.ts` | The reset-to-default route comment claims 'Only the eight stock specialists are valid targets'. STOCK_POD_CONTENT now lists nine pods (researcher, agent-designer, caisson, code-writer, extractor, planner, reviewer, workflow-builder, writer) plus the separately-seeded orchestrator. The route logic is correct (it gates on origin==='stock', not a count), but the comment is stale and will mislead the next reader about the canonical roster size. Pure documentation drift, no behavioral impact. |
| 5 | low | Stale comment in contract.ts claims deleted v1 union still exists | `packages/domain/src/contract.ts` | The header comment says: 'The legacy v1 union stays alive in `work-item-contract.ts` (re-export shim) until 014 supersedes it.' But on this branch `packages/domain/src/work-item-contract.ts` was DELETED (slice 023) and domain/index.ts now re-exports the contract types from contract.ts directly. The comment is misleading for the next reader and contradicts the actual state. Pure documentation drift — no runtime effect, but it points at a file that no longer exists. |
| 6 | low | Stale crash-semantics comment in diagnostics.ts contradicts the supervisor | `apps/server/src/diagnostics.ts` | Comment: 'a non-75 exit makes the dev supervisor log the failure and NOT respawn-loop'. The actual dev-supervisor (scripts/dev-supervisor.mjs) DOES auto-recover non-75 (crash) exits when the server had been healthy >= CRASH_HEALTHY_UPTIME_MS (30s), respawning up to MAX_CRASH_RESTARTS=3 times. So a crash after a healthy boot is respawned, not left dead. The exit(1) behavior is fine; only the comment's description of supervisor behavior is wrong, which could mislead someone reasoning about crash recovery. |
| 7 | low | broadcastAll does not prune dead sockets (asymmetric with broadcast) | `apps/server/src/services/websocket-hub.ts` | `broadcast()` prunes closed/closing sockets from the subscriber Set as it iterates (and deletes empty project entries). `broadcastAll()` merely SKIPS non-OPEN sockets without removing them. Global-scope frames (e.g. project.changed, host-health) go through broadcastAll on the relay path. A socket that closed without a TCP `close` (half-open) lingers in the hub until the 30s keepalive sweep terminates it; in the meantime broadcastAll keeps iterating it every drain tick. Minor inefficiency / inconsistency rather than a correctness bug, since the keepalive sweep is the backstop. Worth aligning the two methods. |
| 8 | low | Agent-host lock-file shape/path/parser duplicated in apps/desktop instead of shared from @pc/runtime | `apps/desktop/src/agent-host-process.ts` | The desktop main process re-declares the agent-host lock-file contract (the PackagedAgentHostLockFile shape with protocolVersion:1, the '<dataDir>/agent-host/host.lock.json' path, and a hand-rolled validator) that already exists canonically in packages/runtime/src/agent-host-lock-file.ts (AgentHostLockFile, AGENT_HOST_LOCK_DIR/FILE constants, agentHostLockFilePath, parseAgentHostLockFile, AGENT_HOST_PROTOCOL_VERSION). These are two sources of truth that must stay in sync: if the agent host (which writes the file via the runtime helpers) ever bumps AGENT_HOST_PROTOCOL_VERSION or changes the path constant, the desktop's hard-coded protocolVersion===1 check and join(dataDir,'agent-host','host.lock.json') silently drift and the packaged shell fails to detect/shut down its own host. The duplication is partly deliberate (desktop has no @pc/runtime dependency to keep Electron's ABI isolated from the runtime's native deps), but the lock-file constants are pure data with no native deps and could be shared via a tiny dependency-free module to remove the drift hazard. |

#### 1. `ephemeral` flag advertised to orchestrator but silently dropped everywhere (deleted feature, stale 4-layer chain)

- **Severity:** medium · **Confidence:** high · **Area:** Server — verification & work items
- **File:** `packages/domain/src/tool-registry.ts` — tool-registry.ts:135-138 (schema); packages/mcp/src/tools/work-items.ts:88; apps/server/src/features/work-items/routes.ts:288,309; apps/server/src/services/agent-work-item.ts CreateAgentWorkItemInput
- **Issue:** The `pc_create_agent_work_item` tool schema still advertises an `ephemeral` boolean promising 'auto-archive 24h after reaching done.' The MCP handler (work-items.ts:88) forwards it, and the HTTP route (routes.ts:309) forwards `ephemeral` into the CreateAgentWorkItemInput object. But the contract-first refactor deleted the `ephemeral` work_items column (migration 0039), deleted ephemeral-work-item-sweep.ts (the only consumer), and `CreateAgentWorkItemInput`/`createAgentWorkItem` have NO `ephemeral` field. The value is accepted at three layers then silently discarded; the advertised auto-archive behavior no longer exists. The orchestrator is being told a feature works when it is fully dead.
- **Evidence:** Read agent-work-item.ts CreateAgentWorkItemInput (lines 36-48) — no `ephemeral` member. Grepped `ephemeral` repo-wide: live (non-archive) refs are only the tool schema, MCP handler, the route pass-through, plus refactor docs that explicitly say 'kills the ephemeral hack' / 'DELETE ephemeral'. ephemeral-work-item-sweep.ts deleted (git status shows D); grep `sweepEphemeral|ephemeral-work-item-sweep` finds only archived tests/docs.
- **Verifier:** Confirmed on every link of the chain. (1) tool-registry.ts:135-138 still advertises `ephemeral` boolean ("auto-archive 24h after reaching done"). (2) MCP handler packages/mcp/src/tools/work-items.ts:88 forwards it into the POST payload. (3) HTTP route apps/server/src/features/work-items/routes.ts:288 types it and :309 spreads `{ephemeral: body.ephemeral}` into the `input` object via a conditional spread — which bypasses TS excess-property checking, so it compiles even though the target type lacks the field. (4) CreateAgentWorkItemInput (agent-work-item.ts:36-48) has NO `ephemeral` member, and …

#### 2. Contract v2 union duplicated in @pc/domain and @pc/contracts with no drift guard

- **Severity:** medium · **Confidence:** high · **Area:** packages/domain
- **File:** `packages/contracts/src/contracts.ts` — ExpectedOutput / Deliverable / AcceptancePredicate / EXPECTED_OUTPUT_KINDS / DELIVERABLE_KINDS / CONTRACT_STATUSES / VERIFICATION_* (lines 26-160), mirroring packages/domain/src/contract.ts
- **Issue:** The entire v2 contract union (the 7 deliverable mechanisms, the 13 acceptance-predicate kinds, the status/tier enums, and the is* guards) is hand-copied in packages/contracts/src/contracts.ts as a deliberate browser-bundle mirror of packages/domain/src/contract.ts (web can't import @pc/domain). The two are byte-equivalent today, but there is NO automated drift test keeping them in sync (unlike the documented pod-tool-catalog drift test at apps/server/test/pod-tool-catalog-drift.test.ts that guards the analogous TOOLS vs PC_RIG_TOOL_NAMES mirror). Add a predicate/kind to only one side and the server-side derivation/evaluation will silently diverge from what the browser DTO can represent. This is exactly the dual-source-of-truth footgun pattern, missing its guard.
- **Evidence:** Read both files: contract.ts ACCEPTANCE_PREDICATE_KINDS (13 entries) and contracts.ts AcceptancePredicate union (13 variants) match; DELIVERABLE_KINDS/EXPECTED_OUTPUT_KINDS/CONTRACT_STATUSES identical. contracts.ts header comment (lines 3-6) explicitly states it MIRRORS @pc/domain for boundary purity. Grepped for a drift/parity test referencing both — none found (only the unrelated pod-tool-catalog-drift test exists).
- **Verifier:** Verified the claim holds. (1) Duplication is real: packages/contracts/src/contracts.ts lines 26-160 and packages/domain/src/contract.ts are byte-equivalent for VERIFICATION_TIERS/STATUSES, DELIVERABLE_KINDS, EXPECTED_OUTPUT_KINDS, CONTRACT_STATUSES, the ExpectedOutput/Deliverable/AcceptancePredicate unions, and the is* guards. contracts.ts header (lines 3-6) explicitly states it MIRRORS @pc/domain/src/contract.ts for browser-bundle boundary purity. (2) The mirror is load-bearing, not vestigial: web imports the contract union from @pc/contracts (apps/web/src/features/contracts/client.ts, work-l…

#### 3. renderAvailableAgents lists the orchestrator itself as a dispatchable agent

- **Severity:** low · **Confidence:** high · **Area:** Server — pods, orchestrator, tool catalog
- **File:** `apps/server/src/services/pod-variable-renderers.ts` — renderAvailableAgents (lines 32-56)
- **Issue:** The orchestrator's {{AVAILABLE_AGENTS}} roster is rendered from listProjectVisibleAgents, which filters by isProjectDispatchable(agent) = (scope==='project' || origin==='stock'). The orchestrator pod is seeded origin:'stock' (orchestrator-pod-content.ts: name 'orchestrator', scope 'global', origin 'stock'), so it passes the filter and appears in its own dispatch list. The renderer applies no self/orchestrator exclusion. Result: the orchestrator's prompt (which embeds {{AVAILABLE_AGENTS}} at orchestrator-pod-content.ts line 122) advertises 'orchestrator' as a dispatch target, and resolveAgentForDispatch('orchestrator') in @pc/db likewise resolves it as dispatchable. The orchestrator could be told it can dispatch itself.
- **Evidence:** pod-variable-renderers.ts:36 calls listProjectVisibleAgents and only sorts/formats — no name=='orchestrator' guard. packages/db/src/repos/pods.ts:1084 isProjectDispatchable returns true for any origin==='stock'. orchestrator-pod-content.ts lines 288-290 confirm origin:'stock'. orchestrator-pod-content.ts:122 embeds {{AVAILABLE_AGENTS}}. Same shared filter feeds resolveAgentForDispatch (pods.ts:1108), so dispatch resolution is also not orchestrator-excluded.
- **Verifier:** Verified every link in the claim's chain against the actual code:

1. renderAvailableAgents (pod-variable-renderers.ts:32-56) calls listProjectVisibleAgents(projectId) and only sorts/formats. No name=='orchestrator' or self exclusion. Confirmed.
2. listProjectVisibleAgents (pods.ts:1092-1097) -> listAgents({projectId, includeGlobals:true}) then .filter(isProjectDispatchable). listAgents (pods.ts:211-236) has NO orchestrator exclusion — returns all non-deleted rows incl. scope='global'.
3. isProjectDispatchable (pods.ts:1084-1086) returns true when scope==='project' OR origin==='stock'. Confirm…

#### 4. Stale 'eight stock specialists' comments after roster grew to nine

- **Severity:** low · **Confidence:** high · **Area:** Server — pods, orchestrator, tool catalog
- **File:** `apps/server/src/routes/pod-routes.ts` — reset-to-default route comment (lines 454-456)
- **Issue:** The reset-to-default route comment claims 'Only the eight stock specialists are valid targets'. STOCK_POD_CONTENT now lists nine pods (researcher, agent-designer, caisson, code-writer, extractor, planner, reviewer, workflow-builder, writer) plus the separately-seeded orchestrator. The route logic is correct (it gates on origin==='stock', not a count), but the comment is stale and will mislead the next reader about the canonical roster size. Pure documentation drift, no behavioral impact.
- **Evidence:** stock-pod-seed.ts:1286-1296 STOCK_POD_CONTENT has 9 entries; orchestrator is seeded separately (orchestrator-pod-seed.ts). pod-routes.ts:455 comment says 'eight'. The route itself uses existing.origin !== 'stock' (line 463) so behavior is roster-size-agnostic.
- **Verifier:** Verified directly. pod-routes.ts lines 454-456 contain the comment "Only the eight stock specialists are valid targets". STOCK_POD_CONTENT in stock-pod-seed.ts lines 1286-1296 contains exactly 9 entries: RESEARCHER, AGENT_DESIGNER, CAISSON, CODE_WRITER, EXTRACTOR, PLANNER, REVIEWER, WORKFLOW_BUILDER, WRITER. The comment says eight but the roster is nine, so it is genuinely stale. Confirmed the route logic gates on existing.origin !== 'stock' (line 463), not on any count, so there is zero behavioral impact — it remains correct regardless of roster size. This is pure documentation drift, low sev…

#### 5. Stale comment in contract.ts claims deleted v1 union still exists

- **Severity:** low · **Confidence:** high · **Area:** Server — core infra (channels, boot, fs)
- **File:** `packages/domain/src/contract.ts` — lines 14-15
- **Issue:** The header comment says: 'The legacy v1 union stays alive in `work-item-contract.ts` (re-export shim) until 014 supersedes it.' But on this branch `packages/domain/src/work-item-contract.ts` was DELETED (slice 023) and domain/index.ts now re-exports the contract types from contract.ts directly. The comment is misleading for the next reader and contradicts the actual state. Pure documentation drift — no runtime effect, but it points at a file that no longer exists.
- **Evidence:** git status shows `D packages/domain/src/work-item-contract.ts`. domain/index.ts:13-25 comment confirms 'the v1 work-item-contract.ts union is deleted' and re-exports from './contract.ts'. contract.ts:14 still references the deleted file as live.
- **Verifier:** Confirmed all three factual pillars of the claim. (1) packages/domain/src/work-item-contract.ts is deleted on this branch: git status shows it as deleted and `ls` returns "No such file or directory". (2) contract.ts lines 14-15 verbatim: "The legacy v1 union stays alive in `work-item-contract.ts` (re-export shim) until 014 supersedes it." — pointing at the now-deleted file as if live. (3) The sibling index.ts lines 13-16 explicitly contradicts it: "Slice 023 — the v1 `work-item-contract.ts` union is deleted. The contract (`contract.ts`, the v2 7-mechanism union) is the single authority." and r…

#### 6. Stale crash-semantics comment in diagnostics.ts contradicts the supervisor

- **Severity:** low · **Confidence:** high · **Area:** Server — core infra (channels, boot, fs)
- **File:** `apps/server/src/diagnostics.ts` — lines 59-61 (uncaughtException handler)
- **Issue:** Comment: 'a non-75 exit makes the dev supervisor log the failure and NOT respawn-loop'. The actual dev-supervisor (scripts/dev-supervisor.mjs) DOES auto-recover non-75 (crash) exits when the server had been healthy >= CRASH_HEALTHY_UPTIME_MS (30s), respawning up to MAX_CRASH_RESTARTS=3 times. So a crash after a healthy boot is respawned, not left dead. The exit(1) behavior is fine; only the comment's description of supervisor behavior is wrong, which could mislead someone reasoning about crash recovery.
- **Evidence:** diagnostics.ts:59-61 vs dev-supervisor.mjs header ('Unexpected exits (non-75)... auto-recover when the server had been running healthy') and constants CRASH_HEALTHY_UPTIME_MS=30_000, MAX_CRASH_RESTARTS=3.
- **Verifier:** Verified by reading both files. diagnostics.ts:59-61 comment claims a non-75 exit makes the dev supervisor "log the failure and NOT respawn-loop." The actual supervisor at apps/server/scripts/dev-supervisor.mjs (the exact path the comment cites) contradicts this. Crash path (lines 242-261): on a non-75 exit, if uptime >= CRASH_HEALTHY_UPTIME_MS (30s) it resets crashRestartCount to 0, increments to 1, and since 1 is not > MAX_CRASH_RESTARTS (3) it does NOT exit — it calls setTimeout(spawnApiChild, delay) and respawns. The supervisor's own header (lines 7-10) states non-75 exits "auto-recover wh…

#### 7. broadcastAll does not prune dead sockets (asymmetric with broadcast)

- **Severity:** low · **Confidence:** high · **Area:** Server — core infra (channels, boot, fs)
- **File:** `apps/server/src/services/websocket-hub.ts` — broadcastAll (lines 62-73) vs broadcast (lines 38-60)
- **Issue:** `broadcast()` prunes closed/closing sockets from the subscriber Set as it iterates (and deletes empty project entries). `broadcastAll()` merely SKIPS non-OPEN sockets without removing them. Global-scope frames (e.g. project.changed, host-health) go through broadcastAll on the relay path. A socket that closed without a TCP `close` (half-open) lingers in the hub until the 30s keepalive sweep terminates it; in the meantime broadcastAll keeps iterating it every drain tick. Minor inefficiency / inconsistency rather than a correctness bug, since the keepalive sweep is the backstop. Worth aligning the two methods.
- **Evidence:** Read websocket-hub.ts: broadcast() lines 51-53 delete dead sockets; broadcastAll() lines 67 only `continue`s. The keepalive sweep (websocket-server.ts runWsKeepaliveSweep, 30s) is the eventual cleanup.
- **Verifier:** Read websocket-hub.ts in full. The asymmetry is exactly as described: broadcast() (lines 47-58) prunes dead sockets via set.delete(socket) on readyState !== OPEN and deletes the empty project entry (line 58); broadcastAll() (lines 62-73) only `continue`s on non-OPEN sockets (line 67), never removing them. Confirmed broadcastAll is a real live path (index.ts wrapper, project-routes for global frames like project.changed) — not dead code, also covered by websocket-hub.test.ts. Verified the keepalive backstop is genuine: runWsKeepaliveSweep (websocket-server.ts) runs every 30s, terminate()s half-…

#### 8. Agent-host lock-file shape/path/parser duplicated in apps/desktop instead of shared from @pc/runtime

- **Severity:** low · **Confidence:** high · **Area:** packages/mcp, runtime, utils, desktop
- **File:** `apps/desktop/src/agent-host-process.ts` — PackagedAgentHostLockFile interface, packagedAgentHostLockFilePath(), readPackagedAgentHostLockFile(), isPackagedAgentHostLockFile()
- **Issue:** The desktop main process re-declares the agent-host lock-file contract (the PackagedAgentHostLockFile shape with protocolVersion:1, the '<dataDir>/agent-host/host.lock.json' path, and a hand-rolled validator) that already exists canonically in packages/runtime/src/agent-host-lock-file.ts (AgentHostLockFile, AGENT_HOST_LOCK_DIR/FILE constants, agentHostLockFilePath, parseAgentHostLockFile, AGENT_HOST_PROTOCOL_VERSION). These are two sources of truth that must stay in sync: if the agent host (which writes the file via the runtime helpers) ever bumps AGENT_HOST_PROTOCOL_VERSION or changes the path constant, the desktop's hard-coded protocolVersion===1 check and join(dataDir,'agent-host','host.lock.json') silently drift and the packaged shell fails to detect/shut down its own host. The duplication is partly deliberate (desktop has no @pc/runtime dependency to keep Electron's ABI isolated from the runtime's native deps), but the lock-file constants are pure data with no native deps and could be shared via a tiny dependency-free module to remove the drift hazard.
- **Evidence:** apps/desktop/package.json dependencies has no @pc/runtime. agent-host-process.ts:5-11,51-53,175-185 redefine shape/path/validator. packages/runtime/src/agent-host-lock-file.ts:6-32,104-133 are the canonical versions, both producing identical path and protocolVersion 1. Both hard-code protocolVersion 1 independently.
- **Verifier:** Confirmed by reading both files and tracing the write/read split. apps/desktop/src/agent-host-process.ts re-declares the lock-file contract independently: PackagedAgentHostLockFile shape with protocolVersion:1 (lines 5-11), packagedAgentHostLockFilePath -> join(dataDir,'agent-host','host.lock.json') (lines 51-53), and a hand-rolled validator isPackagedAgentHostLockFile hard-coding lock.protocolVersion === 1 (lines 175-185). The canonical version lives in packages/runtime/src/agent-host-lock-file.ts: AgentHostLockFile shape, AGENT_HOST_LOCK_DIR/FILE constants, agentHostLockFilePath -> resolve(d…

---

## Dead code (43)

Grouped by subsystem. All verified to have zero production callers.

### Server — agent run lifecycle (5)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | implicit-pause-watcher.ts is entirely unwired (orphaned safety-net service) | `apps/server/src/services/implicit-pause-watcher.ts` | This module is the documented backstop for the 'agent called pc_ask_* but emitted no closing assistant text' case — its header says 'the orchestration layer constructs one watcher per active AgentRun at register time.' Nothing ever does. The exported factory createImplicitPauseWatcher and both interfaces have zero callers anywhere in the repo (apps, packages, templates, scripts, tests). The whole file (~114 lines) is dead, and the implicit-pause safety net it was meant to provide does not exist in the running system. |
| 2 | medium | Four agent-audit history writers are dead — the agent-comms Activity audit trail is never recorded | `apps/server/src/services/agent-audit.ts` | agent-audit.ts is the Section 16b.7 audit-trail writer: one function per agent-comms event that appends a WorkItemHistoryEntry so the work-item Activity tab shows pause/answer/completed/failed. Only recordAgentInvoke is actually wired (routes.ts). The other four writers have no production caller, so the agent-ask-orchestrator / agent-ask-user / agent-approval-request / agent-answer / agent-completed / agent-failed history kinds are never written for dispatched agents — the audit feature is effectively dead, and the four exported functions (~80 lines) are dead code. |
| 3 | low | Three agent-event-header body builders (asks-orchestrator/asks-user/approval) are superseded and dead | `apps/server/src/services/agent-event-header.ts` | These three exported channel-body composers (~120 lines) have no production caller. The live explicit-pause path (pause-resume.ts) builds its ask/approval bodies with its own local buildPauseEventBody() and imports only buildAgentEventHeader from this module. The three builders survive from the v1 channel-delivery era; the only reference is a doc comment in pause-resume.ts and an archived test. |
| 4 | low | evaluateIdle is exported but only used by a test; production stall logic is inlined | `apps/server/src/services/agent-run-idle.ts` | evaluateIdle(idleMs) maps an idle duration to 'ok'|'warn'|'kill'. No production code calls it: the in-process liveness sweep compares computeIdleMs(...) > idleTimeoutMs directly, and the stall-warn sweep computes quiet = idleMs > warnMs inline. The only consumer is apps/server/test/agent-run-stall-warn.test.ts. The sibling exports computeIdleMs/resolveStallWarnMs/resolveIdleTimeoutMs ARE used in production, so this is a dead export, not a dead file. |
| 5 | low | setRunRegistryForTest is an unused test seam | `apps/server/src/services/agent-run-factory.ts` | Exported test-only override for the in-process AgentRunRegistry cap singleton. No test or production code references it (tests inject via deps.runRegistry instead). The internal getRunRegistry() it pairs with IS used. Minor dead export. |

### Server — verification & work items (4)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | Three workflow-step dispatchers (create/update/attach work-item) are orphaned — no engine calls them | `apps/server/src/services/create-work-item-step.ts` | These three exported step dispatchers handle the v1 workflow node kinds CreateWorkItemNode / UpdateWorkItemNode / AttachToWorkItemNode (defined in domain/workflow.ts). They were invoked by the v1 WorkflowRuntime, which no longer exists (workflow-runtime.ts is gone; only comment references remain). The active engine is the v2 dag-run-service, which dispatches only subagent / bash-node-python / orchestrator-review / human-review node kinds — never these. No caller, no live test references them; they are dead orphaned handlers. |
| 2 | low | validateSubagentOutput is never called | `apps/server/src/services/subagent-output-validation.ts` | The exported `validateSubagentOutput` (Section 4h output-schema validator for `pc_complete_node`) has no callers in the live tree. The `pc_complete_node` / output_schema validation path it was built for does not invoke it (grep of packages/runtime/src for output_schema/outputSchema/validateSubagent yields nothing). Its only reference is an archived test. Dead service function. |
| 3 | low | getBackfilledContractForWorkItem exported but unused; encodes the now-broken contract.id==work_item.id assumption | `packages/db/src/repos/contracts.ts` | This repo helper resolves a contract by treating the work-item id as the contract id (the pre-refactor backfill's deterministic id equality). The contract-first refactor explicitly breaks that 1:1 coupling, and the switchover trace doc says to DELETE this function. It is re-exported from db/index.ts but has zero consumers. Leaving it live invites a future caller to re-adopt the broken id-equality assumption. |
| 4 | low | VerificationReviewError cause 'not-agent-task' is never thrown | `apps/server/src/services/agent-verification-review.ts` | The 'not-agent-task' error cause is declared in the VerificationReviewError union and mapped to HTTP 400 in the route's verificationReviewStatus table, but it is never constructed/thrown anywhere. It is a leftover from the pre-contract design when approve/reject gated on wi.isAgentTask; the contract-first guard (loadVerifyingContract) now throws 'wi-not-found' / 'not-awaiting-verification' instead. Dead branch in both the union and the status map. |

### Server — DAG & workflow engine (4)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | low | Entire v1 workflow-runtime step/substitution layer is orphaned (8 files) | `apps/server/src/services/http-step.ts` | These eight files are the v1 WorkflowRuntime dispatch + substitution layer (tagged 4a.4/4a.5/4h/D17-D20). The class they served, WorkflowRuntime, no longer exists — only stale comment references to it remain (project-runtime.ts header, dag-run-service.ts comment). The live workflow engine is now the v2 DagExecutor (dag-executor.ts) which uses its own substituteRefs/RefResolver grammar from @pc/workflows. The v1 step files only import each other (SubstituteTemplate type from typed-substitution.ts) and are exercised solely by suites under archive/tests. They are exported but no production code path calls runHttpStep, runWriteToWorktreeStep, runCreate/Update/AttachToWorkItemStep, shouldRetry, detectRetryCause, makeTemplateSubstituter, applyTypedPortEdges, substituteOutputs, or evaluateBoolean. |
| 2 | low | workflow-event-header.ts (buildWorkflowEventHeader) is unused v1 channel machinery | `apps/server/src/services/workflow-event-header.ts` | This module builds the legacy '[pc:workflow-event kind=... version=...]' channel header (tagged 4c/D38) for the old workflow->orchestrator Channel path. The v2 review delivery now goes through the mailbox seam (deliverReview/deliverWorkflowReview) and the durable workflow.review.changed fact — not a prose channel header. Nothing imports the export. |
| 3 | low | WorktreeService scratch-dir lifecycle (ensure/wipe/sweep + SCRATCH_SWEEP_MAX_AGE_MS) is never invoked by the engine | `apps/server/src/services/worktree.ts` | These methods implement the v1 '4a.8/D18' worktree scratch-dir setup, scratch_cleanup:auto wipe, and boot-time stale-scratch sweep. The v2 DagExecutor + dag-run-service worktree path (fireDagWorkflow -> worktrees.ensureWorktree, runCommand cwd=worktreePath) never calls any of them, and the related ephemeral-work-item-sweep service was deleted on this branch. ensureScratchDir is only called from wipeScratchDir; the other three have no caller outside archive/tests. scratch_cleanup never appears in live src. |
| 4 | low | failure-policy exports withTransientRetry + classifyThrow have no production caller | `apps/server/src/services/failure-policy.ts` | Of failure-policy.ts's public surface, only isTransient is consumed by live code (features/live-events/routes.ts). withTransientRetry and classifyThrow are referenced only by test/failure-policy.test.ts. The bounded transient-retry wrapper this module was built to provide (T2.1) is never actually applied to any route or DB call in the live tree. Confidence is medium because the module itself is live (isTransient) and these may be intended near-term API; but as shipped they are unused code. |

### Server — pods, orchestrator, tool catalog (2)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | low | Boot-time .mcp.json project sweepers rewriteStaleMcpConfigs + applyNodeLauncherToProjects are never wired | `apps/server/src/services/mcp-config-rewrite.ts` | Two exported boot-time migration sweepers that walk every project folder and rewrite its on-disk .mcp.json (npx->node, and packaged-Electron launcher). Neither is called anywhere in production code. PC's runtime moved to rendering MCP config into per-session scratch dirs (claude-runtime-bundle.ts -> prepareClaudeRuntimeFiles + renderPcMcpBaseline), and the per-config helper applyNodeLauncher (which IS live, imported by claude-runtime-bundle) now does the launcher rewrite at materialize time. The two folder-sweeping wrappers are leftover from when PC scaffolded runtime files into project roots. index.ts does not import mcp-config-rewrite for these (it only references --mcp-config in a comment). They are orphaned migration code. |
| 2 | low | pod-revision.ts is an unused re-export of @pc/db helpers | `apps/server/src/services/pod-revision.ts` | This module exists only to re-export computePodRevision / podRevisionsDiffer 'for ergonomic imports inside apps/server'. No file in apps/ actually imports from services/pod-revision. The live consumers (pause-resume.ts:40, agent-run-factory.ts:37) import computePodRevision directly from '@pc/db'. The re-export is dead. |

### Server — core infra (channels, boot, fs) (2)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | low | ProjectRegistry.slugOf + slugById map are dead (zero readers) | `apps/server/src/services/project-registry.ts` | ProjectRegistry maintains a projectId->slug cache (`slugById`) across loadAll/ensure/register/refresh/remove, exposed via the public `slugOf()` method. `slugOf()` has zero callers anywhere in the repo, and `slugById` is only ever read by `slugOf`. The whole cache + accessor is dead weight (the comment claims it's a 'hot path' for 'future channel routing', but that consumer never materialized). Slugs are resolved elsewhere via `runtime.project.slug` / `getProjectBySlug`. |
| 2 | low | Empty orphaned feature directory apps/server/src/features/worktrees/ | `apps/server/src/features/worktrees/` | There are two worktree feature dirs. The live one is `features/project-worktrees/routes.ts` (registered in index.ts as registerWorktreeRoutes). `features/worktrees/` is completely empty (no files) and nothing imports it — a leftover from a rename/move. Harmless but misleading: a reader looking for worktree routes may open the empty dir. |

### Web — work items, contracts, agents (3)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | agent-runs/live-events.ts scan helpers are unused by production (only the unit test imports them) | `apps/web/src/features/agent-runs/live-events.ts` | This 'canonical agent-run live-event consumption' module is not wired into any component or hook. The actual Running-agents feeder (apps/web/src/hooks/use-project-agent-runs.ts) folds agent-run frames via the generic useResourceList + isAgentRunChangedLivePayload, not via scanAgentRunLiveEvents/shouldAcceptAgentRunWsEnvelope/agentRunChangedLiveEventFromUnknown. The module is parallel dead infrastructure kept alive only by its test. |
| 2 | low | focus-agent feature is fully orphaned (no server routes, no UI consumers) | `apps/web/src/features/focus-agent/client.ts` | The entire focus-agent slice (focusAgentApi with getFocusAgentSession / getFocusAgentRuntime / listFocusAgentSessions / getFocusAgentSessionEvents / startNewFocusAgentSession / resumeFocusAgentSession, plus FocusAgentSession / FocusAgentRuntimeSnapshot / FocusAgentSessionTransitionResponse types) is dead. It is only imported into the central api aggregate in apps/web/src/api/client.ts (spread as ...focusAgentApi and re-exported via export *), but nothing actually calls any of its methods, and the server has zero handlers for the /api/focus-agent/* paths these methods hit — so every method would 404 if ever called. |
| 3 | low | attachmentChangedFromLiveFrame is exported but never imported | `apps/web/src/features/work-items/attachment-live-events.ts` | This exported helper (detect a relay live-event frame carrying attachment.changed and yield {workItemId, reason}) has no importers anywhere, including tests. The sibling export in the same file, hasNewAttachmentFrameFor, IS used (WorkItemDetailModal, InitiativeInspector), so only this one function is dead. |

### Web — workflows, chat, live, sessions (5)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | low | Workflow live-event scan helpers are orphaned (only their test references them) | `apps/web/src/features/workflows/live-events.ts` | After migrating workflow-run live consumption to useResourceList + extractFromLiveEvent (use-project-workflow-v2-runs.ts), the whole scan machinery in this file is unused by any production component. Only `shouldCloseWorkflowBuilder` (also exported here) is still consumed (by WorkflowBuilderModal). The scan helpers are exercised solely by apps/web/test/workflow-live-events.test.ts. |
| 2 | low | Mailbox live-event scan helpers are orphaned (only their test references them) | `apps/web/src/features/mailbox/live-events.ts` | The mailbox inbox migrated to the reconcile-first useResourceList path; nothing in src imports this scan module. MailboxInbox.tsx does not reference live-events at all. The refactor tracker (build-slices/T3.1-migrate-stragglers.md) even notes 'scanMailboxLiveEvents left in tree — intentional (reconcile-first; T3.3)', i.e. parked dead code awaiting deletion. |
| 3 | low | Unused workflow-run WS envelope types in ws-types.ts | `apps/web/src/features/runtime/ws-types.ts` | Both interfaces describe the retired `workflow-v2-run-changed` / `workflow-run-changed` envelopes. No code imports either type. WorkflowsList.tsx defines its own local `V2RunChangedEnvelope` instead of importing WorkflowV2RunChangedEnvelope, and the canonical run path uses contracts' WorkflowRunDto. They are leftover wire-shape types from before the live-event spine cutover. |
| 4 | low | Unused orchestrator send-queue cancel/retry client methods | `apps/web/src/features/runtime/client.ts` | These two client methods POST to /send-queue/:id/cancel and /retry but have no caller in the web app. The send-queue UI that would have driven them is not present (no component imports these). They mint routes for a feature with no front-end surface. |
| 5 | low | Unused mailbox deliveries client method | `apps/web/src/features/mailbox/client.ts` | listDeliveries (GET /api/projects/:id/mailbox/deliveries, returning MailboxDeliveryDto[]) has no caller in the web app. MailboxDeliveryDto is re-exported by features/mailbox/types.ts but never consumed by a component either. Dead client surface. |

### Web — shell, store, hooks, API client (6)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | Remote-control corner indicator is dead — mount removed, component + hook orphaned | `apps/web/src/components/RemoteControlCorner.tsx` | The floating "remote" chip (Section 31.10) that shows when /remote-control is active can never render. Commit e74e1cce (37.13 "Chat about this" bridge) deleted both the import and the `<RemoteControlCorner events={events} />` mount from Orchestrator.tsx, but left the component file and its sole consumer hook behind. RemoteControlCorner is now imported by nothing, and useRemoteControl is imported only by RemoteControlCorner — a closed dead loop. The remote-control indicator feature is silently disabled. |
| 2 | medium | Abilities tray is a fully orphaned island (component + store + lib) | `apps/web/src/components/AbilitiesTray.tsx` | The Abilities tray (the ⚡ / '/'-key command palette above the composer) is built but never mounted. The <AbilitiesTray> component is imported by nothing. Its open/close store useAbilitiesTray is referenced only at its own definition. lib/abilities.ts (the builtin-ability catalog + fuzzy filter helpers) is imported only by the dead AbilitiesTray component. All three files reference only each other — a dead island. Note: AbilitiesTray.tsx doesn't even import its own useAbilitiesTray store, so the store was already detached. |
| 3 | low | useGlobalUsageToday hook is never called | `apps/web/src/hooks/use-global-usage-today.ts` | Hook built for Section 31.11 to show today's global usage aggregate in the top header. The header it targeted (App.tsx) now renders only the orchestrator model (telemetryModel) and host-health pill; the usage roll-up was never wired in. The hook polls /api/usage/aggregate but has zero callers, so the polling/refetch logic is fully dead. |
| 4 | low | useProjectStages hook has no consumers despite active maintenance | `apps/web/src/hooks/use-project-stages.ts` | Version-aware project-stages store hook. It has been actively migrated through several slices (Slice 015b relay, Slice 018 off positional scans, last touched 2026-06-01) yet has zero consumers anywhere — no component, page, or test imports it. Either the stages UI surface that would consume it was removed/never built, or it is staged for a future slice. As of this branch it is dead code being carried (and maintained) without a caller. |
| 5 | low | Slice-022 project-contracts hook + API wired but consumed by no UI | `apps/web/src/hooks/use-work-item-contracts.ts` | Slice 022 added a project-scoped, WI-optional contract list: web hook useProjectContracts, client method getProjectContracts, and the server route GET /api/projects/:id/contracts. The server endpoint and the web fetch/live-overlay plumbing exist, but no component calls useProjectContracts, so the WI-optional contract list never renders. This is freshly-landed plumbing missing its consumer rather than abandoned code — may be intentionally staged for the next UI slice. |
| 6 | low | Rich-link invalidation helpers invalidateByFilePath and invalidateRichLink are never called | `apps/web/src/hooks/use-rich-link-data.ts` | Two exported cache-invalidation helpers in the rich-link data module have no callers. The actual invalidator (use-rich-link-invalidator.ts) only dispatches invalidateByWorkItemId and invalidateByAttachmentId; the by-file-path and generic-key variants are exported but unused anywhere, including tests. |

### packages/domain (5)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | workflow-catalog.ts and workflow-ports.ts have no production consumers (legacy v1 workflow surface) | `packages/domain/src/workflow-catalog.ts` | Both modules are part of the legacy v1 workflow authoring surface (closed-world variable catalog + per-node port schemas). After Section 19 (v2 workflows in workflow-v2.ts), nothing in apps/, packages/ (non-archive), templates/, or scripts/ imports these symbols. Only archived tests reference them. They are exported from the index.ts barrel but reachable by no live caller. |
| 2 | low | agent-body.ts is entirely dead — v1 prompt-templating superseded by the pod materializer | `packages/domain/src/agent-body.ts` | This whole module (the v1 `{{input}}/{{wi.body}}/...` agent-body renderer) has no production consumer. The live agent-dispatch path renders prompts in packages/runtime/src/pod-materializer.ts, which uses its OWN Section-36 `{{KEY}}` substitution and never imports agent-body.ts. Every symbol is re-exported from index.ts but consumed by nobody. Safe to delete (or it should be, once index.ts re-exports are dropped too). |
| 3 | low | workflow-edges.ts runtime helpers (parseEdgeRef/formatEdgeRef/isCompactEdgeRef) are dead; only the types are used | `packages/domain/src/workflow-edges.ts` | The EdgeRef / NodeEdges TYPES from this file are live (apps/server/src/services/typed-substitution.ts imports them as types). But the three exported runtime functions have zero callers in production — typed-substitution.ts only MENTIONS isCompactEdgeRef/parseEdgeRef in a comment (line 69) and implements its own resolveEdgeRef instead. The functions are referenced only by archive/tests/packages/domain/workflow-edges.test.ts. |
| 4 | low | Unused v2 workflow node type-guards (isWorkflowNodeKind, isAgentNode, isBashNode, isScriptNode) | `packages/domain/src/workflow-v2.ts` | workflow-v2.ts is heavily consumed via the WorkflowV2.* namespace (Workflow, WorkflowNode, WorkflowDagState, isReviewNode, NodeRunState, etc. all used in apps/web + apps/server). But these four specific guards/predicates have no caller anywhere. Only isReviewNode is used (apps/web workflow-layout.ts + WorkflowGraphV2.tsx). Low severity because the module is explicitly slated for active coexistence until the 19.12 cull, but these four guards are currently dead weight. |
| 5 | low | Stale comment in pod-defaults.ts claims a module-load assertion that does not exist | `packages/domain/src/pod-defaults.ts` | The comment on requiresWorkItem says the flag is 'asserted at module load so the two never drift.' There is no assertion at module load — podDefault() simply DERIVES requiresWorkItem from expectedOutputRequiresWorkItem(), so drift is structurally impossible and no assertion is needed. The claim is a leftover from an earlier design where the flag was hand-authored. Harmless (the value is correct) but misleading to a future reader who may go hunting for the guard. Trim the comment. |

### packages/db (1)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | low | getBackfilledContractForWorkItem is dead — read-through shim for a removed coupling, zero callers | `packages/db/src/repos/contracts.ts` | This function existed only as the read-through shim for the old 'a contract's id equals its work item's id' backfill coupling (migration 0038's deterministic ids). The refactor/auto-pathway branch finished the contract-first switchover and migration 0039 dropped the nine legacy work_items contract columns, so the id-equality assumption is gone. The function (which just returns getContract(workItemId)) is now unreferenced anywhere — it is a public-API export that nothing consumes. The refactor plan explicitly calls for deleting it ('DELETE getBackfilledContractForWorkItem; new code must never assume the equality') but it and its index re-export were left in place. Keeping it is a footgun: any new caller that uses it silently re-introduces the contract.id == work_item.id assumption the refactor removed. |

### packages/app-services & contracts (4)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | Agent-invoke/continue request parsers + interfaces are dead; the live route hand-parses and has already drifted | `packages/contracts/src/agent-runs.ts` | These four exported symbols are the intended validators for the agent invoke/continue HTTP routes, but the real route at apps/server/src/features/agent-runs/routes.ts (l.409-444, 505-521) hand-rolls its own inline body parsing and never calls them. They are dead production code. Worse, they have already drifted from the live contract: the route reads an `expectedOutput` field that InvokeAgentRequest/parseInvokeAgentRequest don't model at all, so anyone who later wired the route to the parser would silently strip the contract spec. This is exactly the dual-source-of-truth footgun. |
| 2 | low | Work-item list-shape helper functions are test-only dead code | `packages/app-services/src/work-items/adapters.ts` | These three exported helpers exist to produce/normalize the two legacy work-item list response shapes ({workItems} and {items,nextCursor}). No production code calls them: the server work-items route builds `{ workItems: items }` inline (routes.ts l.96, l.105). The paged `{items,nextCursor}` shape that toItemsCursorListBody emits is never produced by any route. Only the gateway re-exports the *type* WorkItemListResponse (not the functions), and only the adapters test exercises the functions. |
| 3 | low | ProjectChangedPublication.event is a duplicate of legacyEvent and never read | `packages/app-services/src/projects.ts` | Every ProjectChangedPublication carries both `event` and `legacyEvent`, assigned the identical ProjectChangedRefetchEnvelope. The server project routes only ever read result.legacyEvent and result.liveEvent (routes.ts l.72/87/107/119), never result.event. The `event` field is dead duplicate state. |
| 4 | low | toAttachmentDto is redundantly re-exported and has no production consumer | `packages/app-services/src/work-items/gateway.ts` | toAttachmentDto is already exported package-wide via work-items/index.ts -> adapters.ts (export *). The explicit `export { toAttachmentDto }` at the bottom of gateway.ts is a redundant second export of the same symbol. Separately, the function itself has no production caller (only its own def + this re-export + the adapters test), so the public surface is wider than anything that consumes it. |

### packages/mcp, runtime, utils, desktop (2)

| # | Sev | Title | File | What & why |
|---|---|---|---|---|
| 1 | medium | packages/runtime/src/hook-scripts/ is a dead, stale duplicate of the live template hooks | `packages/runtime/src/hook-scripts/` | The CC hook scripts that PC actually installs at runtime are read exclusively from templates/.claude/hooks by apps/server/src/services/claude-runtime-bundle.ts (prepareClaudeRuntimeFiles reads hookSrcDir = ctx.templatesDir/.claude/hooks and renders each *.cjs into the session-local runtime). The four .cjs files under packages/runtime/src/hook-scripts/ are consumed by NOTHING: runtime has no build/dist step (package main is ./src/index.ts), index.ts does not export them, and no build copies them. They are byte-identical duplicates of four of the template hooks but the runtime copy is STALE — it is missing inbox-drain.cjs and pc-statusline.cjs, which the live template dir has. This is the documented 'hook template trap': anyone who edits the runtime copies (the intuitive 'source' location next to the rest of the runtime code) edits dead files and their change never ships. Delete the directory, or replace it with a pointer comment. |
| 2 | low | packages/mcp/src/capabilities.ts (CAPABILITIES / CAPABILITY_NAMES) is production-dead, referenced only by its own tests | `packages/mcp/src/capabilities.ts` | This module derives a tool-name -> {family} map from PC_RIG_TOOL_REGISTRY. Its stated purpose (slice 011 comment) was a 'lookup layer used to reason about which contract family a tool belongs to' for the typed client / family routing. But the @pc/mcp package only exports src/server.ts (package.json exports field exposes just the '.' entry = server.ts), server.ts never imports capabilities.ts, and the typed client (client/typed-client.ts) does NOT use CAPABILITIES — its 'family' references are only comment section headers. The exact symbols CAPABILITIES and CAPABILITY_NAMES appear nowhere in production code; the only importers are packages/mcp/test/{capabilities,registry-golden,registry-parity}.test.ts. It is a derived registry kept alive solely by parity tests that test itself — no runtime consumer reads it. The CapabilityFamily type IS used (carried on registry records), but the CAPABILITIES const map and CAPABILITY_NAMES are dead. |

---

## Appendix — Refuted candidates (2)

These were proposed by auditors but knocked down on verification — listed so they are not re-investigated.

- **RunInlineDetail live graph overlay listens for a retired envelope shape — never updates live** (`apps/web/src/components/WorkflowsList.tsx`) — REFUTED. The current code in apps/web/src/components/WorkflowsList.tsx (RunInlineDetail, lines 853-909) does NOT scan for type:'workflow-v2-run-changed' and does NOT define a local V2RunChangedEnvelope type. It reads the canonical live frame: useLiveEvents('workflow-run', project.id) (line 885), filters with isWorkflowRunChangedLivePayload(ev.payload) (line 889), and reads the NESTED ev.payload.run (line 890) — exactly the canonical path the auditor's own evidence says is correct (use-project-wo
- **enqueueRuntimeTurn accepts source/sourceRef but silently discards them; mailbox source attribution is lost** (`packages/app-services/src/conversations/send-service.ts`) — Mechanical observation is correct but the characterization ("source attribution is lost", "API that does nothing") does not hold; this is an intentional, documented design, not a misconfig/bug.

What I checked:
- send-service.ts l.188-222: enqueueRuntimeTurn declares source?/sourceRef? but the body (l.205-211 enqueueOrchestratorSend) omits both. Confirmed.
- The caller is at apps/server/src/services/mailbox-orchestrator-turn-adapter.ts (NOT packages/app-services/... as the claim states — wrong p
